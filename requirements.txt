# cats-inbox 執行期依賴(釘版本:重建必須得到同一份東西)
fastapi==0.141.1
uvicorn[standard]==0.34.0
jinja2==3.1.5
itsdangerous==2.2.0
# ── OIDC(T04)──
# PyJWT 的 crypto extra 帶 cryptography:RS256 驗簽必需
# (契約 §3.2 只接受 RS256;沒有 crypto extra 時 PyJWT 只能做 HMAC)
PyJWT[crypto]==2.10.1
httpx==0.28.1

# ── 資料層與後台表單(T05)──
sqlalchemy==2.0.36
alembic==1.14.0
# psycopg 3 的 binary wheel:免在映像裡裝編譯鏈
psycopg[binary]==3.2.3
# FastAPI 處理 <form> POST 需要它(角色後台的派/停角色按鈕)
python-multipart==0.0.20
