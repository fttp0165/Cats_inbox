# 2026-09-23 T14a 推送 API 本體(S2S 驗證 / 來源登記 / 去重)

**建立日期:** 2026-09-23 14:50
**最後更新:** 2026-09-23 15:15
**版本:** v1.1
**對應任務:** **T14a**(M5 系統通知開通;T14 拆分的前半)

> 本篇計畫段於**動工前**寫妥(第二條2)。結果段完工後補在同一檔。
> ⏱ 時間戳來源:開發機 `date` = `2026-09-23 06:48 UTC` → 本檔一律標 UTC+8。

---

## 計畫段(動工前寫妥,第二條2)

### 目標

把 portal 2026-08-18 核定的寫入型 S2S 規格(契約 §11.7 第一案)與我方 2026-08-24 對三問的答覆
(`docs/契約對齊盤點_v3.3.md` §4 Q2/Q3)做成**可離線驗證**的實作:

1. `POST /inbox/api/v1/notifications` —— 驗 S2S token(RS256/JWKS/`iss`/`aud`/`exp`±30s)、
   **逐端點驗 scope `notification:push`**、`azp` 查來源登記表(deny-by-default)、
   `X-User-Id` 必驗、`Idempotency-Key` 去重(24h)、`source_app` 由 `azp` 推導;
2. 來源登記表 `source_app`(`azp` → 顯示標籤 + 啟用旗標)與管理後台 `/inbox/admin/sources`
   (Q2 答覆:「新增來源=管理後台加一列;可停用單一來源而不動程式」);
3. 去重收據表 `push_receipt`;migration **`0003`**;
4. 來源接入指南 `docs/來源接入指南.md` + HTML(給第一個來源系統的團隊照做)。

### 🔴 為什麼現在做,以及為什麼把 T14 拆成 T14a / T14b(第一條4)

任務表 T14 的依賴欄寫著「**第一張憑證待 DI-4 立案**」。但逐條看驗收,**六支規格測試沒有一支需要真的憑證**
—— 它們要的是一張「由 IdP 簽、帶某些 claims」的 token,而 T04 在 client 核發**之前**就用假 IdP 簽出
這種 token 把登入流程做完了。真正需要憑證的只有「**接入第一個來源**」那一步。

這與 T11 拆成 T11a/T11b 是同一個形狀(任務表 v1.21):**阻塞條件不同,合成一列會讓能做的那一半
跟著不能做的一起卡住。** 拆法:

| 列 | 內容 | 阻塞 |
|---|---|---|
| **T14a**(本任務) | API 本體 + 登記表 + 去重 + 來源後台 + 接入指南 + migration `0003` | 無 —— 全部可離線驗證 |
| **T14b** | 接入第一個來源:portal 核發 `<client>-sa` + 我方登記 `azp` + 對方實作 + VM 冒煙 + 含 `0003` 的版本上線 | **DI-4 立案 + 第一張憑證**(portal 與來源系統) |

另外三個未完成的方向現在都做不了:T11b 的 C/D 段(portal repo + Benny 的低峰窗口)、T12(在 T11b 之後)、
T15–T17(DI-3 裁決)。**T14a 是唯一一件不必等任何人的開發工作。**

⚠ **誠實標註(第二條3)**:本任務是新功能 + migration,屬第二條3 的「大任務」。依本專案 T05/T07/T09 的既有做法,
計畫寫在本篇計畫段、規格以 portal 核定與我方答覆為準,**未另立 `docs/plans/`**。
第二條3 要求的「**經確認後才動工**」,本次的依據是 Benny 2026-09-23 的指示「查看專案規畫繼續開發」,
**不是對本篇計畫的逐項確認**。緩解:全部改動只在分支上,**不發版、不碰 VM**,可整段 revert;
若對拆分或下列任何一項設計決定有異議,改動成本只有程式碼。

### 影響範圍

| 對象 | 影響 |
|---|---|
| 資料庫 schema | 🟡 **第三條 migration `0003`**:CREATE TABLE `source_app`、`push_receipt`。🔴 **不對 `0001`/`0002` 的表加任何欄位**(理由見設計決定 9) |
| 既有 API | 🟢 逐字不動。新增 `POST /api/v1/notifications` |
| 既有 UI | 🟡 角色後台加一個連到來源後台的連結;新頁 `/inbox/admin/sources` |
| 授權 | 🟡 `admin` 多一個能力 `manage_sources`。🔴 **`reader` 那一列一個字都不動**(DI-2 條件 C1 的邊界) |
| OIDC | 🟡 新增 `verify_access_token`;`verify_id_token` 的解碼核心抽成共用(既有 12 支 auth 測試 + T04 的突變檢查當安全網,第三條4) |
| portal / VM / gateway | 🟢 **完全不動**。VM 上跑的 `0.1.1` 不含本端點;本任務**不發版** |

### 驗收標準

六條來自任務表 T14 與 `docs/TDD測試計畫表.md`(portal 2026-08-18 核定);其餘是本任務自加的。
**每一條都對應一種不會有錯誤訊息的失敗:**

| # | 驗收 | 壞掉時的症狀(為什麼要有這一條) |
|---|---|---|
| 1 | 無 token / **使用者的 id_token** / 使用者的 access token / 錯 `aud` / 過期 / HS256 → **401** | 🔴 id_token 的 `aud` **正好就是 `cats-inbox`** —— 只驗 `aud` 的話,任何登入過的人拿自己的 id_token 就能以系統身分發通知 |
| 2 | `aud` 對但缺 `notification:push` → **403**(不是 401);scope 比對是**整詞** | §11.5:401/403 混用,呼叫方查不出是憑證錯還是授權不足;子字串比對的話 `notification:pushall` 也算 |
| 3 | 未登記 / 已停用的 `azp` → **403**;請求帶內網來源位址照樣 403 | §11.5 第 3 條:「**內網不是身分**」 |
| 4 | `X-User-Id` 缺 / 非 UUID / 等於呼叫方自身 → **400 且零列** | portal 加嚴條件:「缺就當系統發出」會讓稽核鏈在最需要的時候恰好是空的 |
| 5 | 同 `Idempotency-Key` 24h 內重送 → **200、同一 id、零新列**;缺 key → 400;超窗 → 201 新訊息 | 重試造成同一則通知出現兩次;超窗拒收則讓一個月後的重跑**靜默失敗**(Q3) |
| 6 | body 帶 `source_app` → **被忽略**,以 `azp` 查表的標籤為準 | 能自稱來源 = 能冒充任何系統發通知,而通知帶著平台的官方外觀 |
| 7 | 同 key、**不同內容**(24h 內)→ **422 且零新列** | 呼叫方把 key 重用在另一個事件上時,第二則會被當成重送**默默丟掉** —— 而「通知沒送到」是本系統最糟的失效模式 |
| 8 | key **以呼叫方為範圍**:兩個來源用同一把 key 互不影響 | 否則 A 系統的 key 會讓 B 系統的通知被「回放」成 A 的那一則 —— 內容跨來源外洩 |
| 9 | `recipient_sub` 非 UUID 形狀 → **400 且零列**;大寫 UUID 正規化成小寫 | 傳 email 或帳號名進來會**回 201**,而那則訊息**沒有任何人看得到**(收件匣以 `sub` 比對)—— 靜默遺失 |
| 10 | 外部 `action_url` 經推送端點 → **400 且零列** | T10 把關卡設在 `repo.create_message()`,理由正是「T14 動工時驗證是附帶工作,最容易忘」—— 這條證明 T14 **真的穿過它** |
| 11 | **先認證、後讀 body**:無 token + 壞 JSON → **401** | 否則未認證的呼叫方拿到的是 400/422,而且能用它探測 payload 格式 |
| 12 | 推送的訊息,**收件人登入後真的看得到**,標籤是登記表的 label | 只驗 201 的話,「寫到別人名下」「寫到錯的欄位」都是 201 |
| 13 | log 有 `azp` / actor / `message_id`;**沒有**主旨、內容、token 全文 | A.3 紅線;個資或 token 落進 log **沒有任何人會發現** |
| 14 | 🔴 **列舉式守門:所有寫入路由,匿名請求一律 401/403** | A.3「**無驗證的推送端點不得部署**」目前只是一句話 —— 列舉之後,下一個忘了掛認證的寫入端點會在 CI 紅 |
| 15 | 來源後台:非 admin **403**;登記/停用**即時生效**;壞值 400 零列;表單帶 CSRF(T10b 的列舉守門自動涵蓋) | 停用要等重啟才生效的話,「可停用單一來源而不動程式」(Q2)就是假的 |
| 16 | 並行的同 key 請求:第二個撞唯一約束 → **回放 200,零重複** | 先查後寫之間的空窗,在真實重試風暴裡一定會被撞到 |
| 17 | migration `0003` up→down→up(CI 的 **PG 15**);降級**不動** `0001`/`0002` 的表;`(azp, key)` 唯一約束在 PG 上**真的擋** | 降級手誤帶走 `message` = 刪掉所有人的通知 |

### 🔴 設計決定(先寫下,因為它們會被誤讀成疏漏)

1. **收據表不做每日清理** —— 與 2026-08-24 答覆 Q3 的**內部**描述不同,**對外語意不變**
   (24 小時、超窗視為新訊息)。理由:①本服務刻意沒有排程器(A.1 極簡棧:無佇列、無背景工作),
   「每日 DELETE」需要一個不存在的東西去跑它;②收據同時是 **`X-User-Id` 的持久稽核紀錄**
   —— portal 加嚴那一條的理由正是稽核鏈,**每日刪收據就是每日刪稽核**。
   改為:窗口**以時間比對**判定;收據隨 `message` 保留(外鍵 `ON DELETE CASCADE`,N3 定保留期時一併處理)。
   超窗重用同一把 key → 覆寫該列收據指向新訊息(舊的關聯仍在 log)。→ **回寫 Q3**(第二條5)。
2. **同 key 不同內容回 422**(IETF `draft-ietf-httpapi-idempotency-key-header` 的建議碼),不回放。
   回放會讓呼叫方拿到 200 而**第二則通知不存在**。
3. **`recipient_sub` 也驗 UUID 形狀** —— 核定規格只要求驗 `X-User-Id`;加這一條的理由見驗收 9。
   前提與核定規格相同:平台的 `sub` 是 Keycloak 的 UUID(`X-User-Id` 的 UUID 規則本身就建立在它上面)。
4. **token 的 `azp` 等於我方自己的 client_id → 401**:那是使用者登入流程簽給我方的 token
   —— 我方 client 的 service account **已確認停用**(T03 回讀驗證),不可能是服務憑證。
5. **`typ` 必須是 `Bearer`** —— ~~這是驗收 1 裡「使用者 id_token」那一格**唯一**擋得住的地方~~
   → ⚠ **此句有誤,完工時更正**:決定 4(`azp` 是我方自己 → 401)**也**擋得住 id_token
   (id_token 的 `azp` 就是我方)。兩道是各自獨立的關,見結果段〈發現 1〉。
   Keycloak 的 id_token 是 `typ: ID` 而 `aud` 就是 `cats-inbox`。
6. **body 在認證之後才讀**,且**所有輸入錯誤一律 400**(不是 FastAPI 預設的 422)——
   對齊 `docs/開發計畫書.md` §3.4 的錯誤語意;**422 只留給決定 2**,讓它有單一意義。
7. **scope 名稱與 24h 窗口寫死成常數**,不從設定讀 —— 與 `ROLE_CAPABILITIES`、`SITE_ORIGIN` 同一個理由:
   設定可以被改寬,而改寬沒有症狀。
8. **來源標籤以「快照」寫進 `message.source_app`**:改標籤不回溯既有訊息(與信件的寄件人顯示同理)。
9. 🔴 **不對 `message` 加欄位**(稽核放在 `push_receipt.actor_sub`)—— 上游 §2.1 的退路是「**搬四張表**」;
   在 `message` 上長出 S2S 專用欄位,搬表就變成一次資料遷移。兩張新表是 S2S 路徑專用的,
   併回 compliance 時由其內部呼叫取代,不需要搬。

### 刻意不做(範圍外)

- **T14b**(接入第一個來源)、**發版 `v0.2.0`**(Benny 的決定;含 `0003` 的版本上 VM 前須先備份)。
- 一次推給多個收件人(先一呼叫一收件人;有量再議)、`audience` 群組(需 §4.2a L2)。
- 收據清理工具(見決定 1)。

### 🔴 對現有資料的影響

🟡 **加表**:`CREATE TABLE` ×2(含索引、唯一約束、外鍵)。**不動任何既有表的欄位、無 UPDATE、無刪除。**
VM 上的庫停在 `0002`;`0003` 要等下一次發版才會在 VM 上跑,**本任務不碰 VM**。

### 回滾方式

1. 程式:`git revert` 本任務的提交。
2. migration:`0003` 的 downgrade **只刪兩張新表**(測試釘住 `0001`/`0002` 的表留著)。
   ⚠ 上線後回滾前必先備份(共通紅線)——收據表是稽核紀錄。
3. **不必回版的關閉方式**:推送端點在「沒有任何啟用中的來源」時等於全拒(deny-by-default),
   上線後可在來源後台**停用全部來源**,秒級關閉。

### 估時

6–8 h(任務表 T14 原估;T14b 另計,且大半在對方系統)。

---

## 結果段

**完成時間:** 2026-09-23 15:12(開發機 `date` = `07:12 UTC`)

### 做了什麼(異動檔案)

| 檔案 | 異動 |
|---|---|
| `alembic/versions/0003_push_source_registry.py` | 🆕 `source_app` + `push_receipt`(`(azp, idempotency_key)` 唯一、`message_id` 外鍵 CASCADE、`azp` 外鍵不 CASCADE);downgrade 只刪這兩張 |
| `app/models.py` | 🆕 `SourceApp`、`PushReceipt`;`SOURCE_LABEL_MAX = 32`(= `message.source_app` 欄寬) |
| `app/oidc.py` | 解碼核心抽成 `_decode_rs256`(id_token 與 access token 共用);🆕 `verify_access_token`(`azp` 必備、`typ` 必須 `Bearer`);`OidcError` 可帶 `headers` |
| `app/s2s.py` | 🆕 `require_s2s_scope(scope, own_client_id=…)`:Bearer → 驗 token(401)→ `azp` 不得是我方(401)→ scope 整詞(403)→ 登記表(403);拒絕帶 `WWW-Authenticate`、記 `s2s_rejected` |
| `app/routes_push.py` | 🆕 `POST /inbox/api/v1/notifications`:**先認證後讀 body**、`X-User-Id` / `Idempotency-Key` / `recipient_sub` 驗證、指紋、撞唯一約束時回滾重試(= 回放)、`notification_pushed` / `push_rejected` log;OpenAPI 補 requestBody |
| `app/repo.py` | 🆕 來源登記四函式、`find_push_receipt`、`push_notification`(24h 窗口、422、超窗覆寫收據);檔頭說明更正(見發現 4) |
| `app/validation.py` | 🆕 `IdempotencyKeyReused`(422) |
| `app/authz.py` | 🆕 `CAP_MANAGE_SOURCES`(只給 `admin`);`reader` 那一列**一字未動** |
| `app/routes_admin.py` + `app/templates/admin_sources.html` | 🆕 `/inbox/admin/sources`(清單 + 登記、啟用停用兩個表單,**兩個 POST 皆 CSRF**);角色後台加連結 |
| `app/main.py` | 掛推送路由;錯誤處理帶 `headers` |
| `tests/test_push.py` | 🆕 22 支 |
| `tests/test_schema.py` | +4 支(`0003` 雙向、唯一約束、外鍵、欄寬) |
| `tests/fake_idp.py` | 🆕 `access_token()`(真實 Keycloak client_credentials 的完整 claims)、`user_access_token()`;簽章抽成 `_sign` |
| `tests/conftest.py` | PG 清場清單加兩張新表(漏加的症狀是下一支測試撞 `relation already exists`) |
| `docs/來源接入指南.md` + HTML | 🆕 給來源系統的操作指南;登記進 `render_docs.py` 的 `TARGETS` |

### 測試結果(紅 → 綠)

- **紅**(實作前):`test_push.py` + `test_schema.py` → **25 failed / 10 passed** —— 25 支新測試全紅
  (ImportError / 路由 404 / `relation "source_app" does not exist`),10 支既有 schema 測試仍綠。
- **綠**(本機 PG **16.13**):全 repo **284 項** —— 文件層 **70**(+5:新文件的 metadata / 版本歷史 / HTML / light / 無外部資源)
  + pytest **214 passed / 0 skipped**(188 → 214:`test_push.py` 22 + `test_schema.py` 4)。
  ⚠ PG **15** 的那一輪由 CI(`postgres:15.8-alpine`)跑;本機只有 16。
- 重構的安全網:`_decode_rs256` 抽出後,**auth 12 + 登出 9 + 授權 13 = 34 支既有測試**先跑一輪全綠才往下做。

### 突變檢查:36 / 36 全被抓到

| # | 植入的缺陷 | 抓到它的測試 |
|---|---|---|
| M01 | 拿掉 `typ == Bearer` | `test_push_requires_s2s_token`(`typ=ID` 那一格) |
| M02 | 拿掉「`azp` 是我方自己 → 401」 | 同上(`azp` 是我方那一格:變成 403 而非 401) |
| M03 | scope 改成子字串比對 | `test_push_verifies_scope_per_endpoint`(`notification:pushall`) |
| M04 | 缺 scope 回 401(401/403 混用) | 同上 |
| M05 | 停用的來源照樣放行 | `test_unregistered_azp_is_denied` |
| M06 | 未登記的 `azp` fallback 成「未知來源」 | 同上 |
| M07 | access token 不要求 `azp` | `test_push_requires_s2s_token`(缺 azp:變成 403) |
| M08 | `X-User-Id` 可等於呼叫方自身 | `test_x_user_id_is_verified_not_just_logged` |
| M09 | 缺 `X-User-Id` 就當系統發出 | 同上 |
| M10 | `recipient_sub` 不驗形狀 | `test_recipient_sub_must_be_a_uuid` |
| M11 | UUID 不轉小寫 | `test_x_user_id_is_verified_not_just_logged`(收據的 actor_sub) |
| M12 | 同 key 不同內容照樣回放 | `test_same_key_with_different_payload_is_422` |
| M13 | 窗口邊界 `>` 改 `>=` | `test_idempotency_window_is_24_hours`(正好 24h) |
| M14 | 超窗拒收 | 同上 |
| M15 | 超窗建新訊息但不更新收據 | 同上(第四次應回放新的那一則) |
| M16 | 撞唯一約束不重試 | `test_concurrent_duplicate_key_is_replayed_not_duplicated` |
| M17 | model 的唯一約束改成只有 key | `test_idempotency_keys_are_scoped_per_caller` |
| M18 | migration 的唯一約束改成只有 key | `test_push_receipt_key_is_unique_per_caller`(PG) |
| M19 | `source_app` 寫 azp 而非標籤 | `test_idempotency_keys_are_scoped_per_caller` |
| M20 | body 有 `source_app` 就採信 | `test_source_app_ignores_body` |
| M21 | 拒絕 log 帶上 token | `test_push_log_has_audit_fields_but_no_content_or_token` |
| M22 | 成功 log 帶上主旨 | 同上 |
| M23′ | 先讀 body 再認證 | `test_body_errors_are_400_and_auth_comes_first`(`無 token + 壞 JSON 應 401,實得 400`) |
| M24 | 登記來源不驗 CSRF | `test_csrf.py::test_every_form_post_route_requires_csrf`(T10b 的列舉守門**自動**涵蓋新表單) |
| M25 | `reader` 也能管來源 | `test_sources_admin_requires_admin` |
| M26 | 重複登記默默改標籤 | `test_register_source_rejects_bad_values` |
| M27 | 標籤不限長度 | 同上(SQLite 無視欄寬 → 303) |
| M28 | 切換不存在的來源回 303 | 同上 |
| M29 | 模板:切換表單漏 CSRF | `test_sources_page_is_csp_clean_and_csrf_protected` |
| M30 | 模板:標籤 `|safe` | `test_source_label_is_escaped_on_every_page` |
| M31′ | `0003` 降級多 drop 一張 `0002` 的表 | `test_migration_0003_up_down_up`(`降到 0002 卻把 announcement_read 也刪了`) |
| M32 | migration 的標籤欄寬 64 | `test_source_label_fits_the_column_it_is_copied_into`(PG) |
| M33 | 收據外鍵不 CASCADE | `test_push_receipt_foreign_keys`(PG) |
| M34 | 新增一個沒有認證的寫入端點 | `test_every_write_route_rejects_anonymous` —— **證明列舉守門不是空檢查** |
| M35 | 拿掉 OpenAPI 的 requestBody | `test_openapi_documents_the_push_contract` |
| M36 | `Idempotency-Key` 不驗字元 | `test_idempotency_key_dedupes`(含空白的 key) |

⚠ **M23 與 M31 的第一版「抓到了,但抓到的理由不對」**,改寫成 M23′ / M31′ 才算數:
- M23 第一版把相依參數寫成 `caller: S2SCaller = None`,FastAPI 把 dataclass 型別的參數當成 **body**,
  於是紅在 `test_push_requires_s2s_token` 的**正向對照**——紅了,但不是因為「先讀 body」。
- M31 第一版在 `-x` 下先紅在 `0002` 那支(後續 downgrade 找不到表),不是 `0003` 那支的斷言。
- 兩者都以「**只跑那一支預期的測試、看它紅在哪一行**」重跑:都紅在預期的斷言上(訊息如上表)。
  🔴 **「抓到了」要看是哪一行抓到的** —— 紅在錯的地方的突變,證明的是另一件事。

### 🔴 發現

1. **計畫段決定 5 寫錯了一句,而那句話差點讓一道關卡沒有測試。**
   計畫寫「`typ` 是『使用者 id_token』那一格**唯一**擋得住的地方」。但 id_token 的 `azp` 就是我方,
   所以決定 4(`azp` 是我方 → 401)**也**擋得住它 —— 兩道關都擋同一個案例,**單獨拿掉任一道都不會紅**。
   排突變清單時發現,在跑之前補上 `typ=ID` / `typ=Refresh`(其餘全對、只有 `typ` 擋得住)兩格;
   M01 就是紅在那一格。計畫段該句已以刪除線更正(不改寫原文)。
   ⚠ 這與本專案記過多次的「守門只覆蓋一部分對象」是**姊妹形狀**:那些是一道守門漏了對象;
   這次是**一個案例同時被兩道守門擋住**,於是每一道單獨都沒被測到。
2. **FastAPI 在解析相依之前就讀 body。** 宣告一個 Pydantic body 參數的話,壞 JSON 的請求會在認證**之前**
   拿到 422 —— 未認證的呼叫方能拿本端點探測格式,而「你沒有憑證」被蓋掉。改為在「依賴認證的 async 相依」裡
   手動讀 body(`_authenticated_json`),並以 `openapi_extra` 把 schema 補回 OpenAPI(M35 釘住)。
3. **T10b 的 CSRF 列舉守門在 T14a 第一次「自動涵蓋新表單」** —— 收表單的 POST 路由由 3 支變 5 支,
   沒有人改那支測試;M24 證明它真的會紅。這是 T10b 把守門寫成列舉式的回報。
4. `app/repo.py` 檔頭寫著「唯一會寫 `app_user` / `user_role` 的地方」—— **T07 起就不只了**(訊息、公告),
   一直沒人更正。已更正為「本服務唯一寫資料庫的地方」並註明舊句何時失效。

### 對現有資料的實際影響

🟡 **加表,未在任何有資料的庫上執行**:`0003` 只有 CREATE TABLE,在本機 PG 16.13 雙向演練過
(`0003` up→down(→`0002`)→up,`0001`/`0002` 的五張表留著),PG 15 由 CI 跑。
VM 上的庫仍停在 `0002`、跑的是 `0.1.1`(不含本端點)—— **本任務沒有碰 VM、沒有發版**。

### 遺留問題與後續建議

1. **T14b**(接入第一個來源)等 DI-4 + 第一張憑證。⚠ 動工前先與 portal 定案:
   **沒有人觸發的事件(排程/批次)`X-User-Id` 填誰** —— 核定規格沒有明講(接入指南 §3.1 已誠實標出)。
2. **含 T14a 的版本上 VM 前**:版本常數與 compose tag 升 **`0.2.0`**(新功能,第八條4)、
   Release 頁「對現有資料的影響」寫 🟡 加表 + **要跑 migration**、**先備份**(共通紅線)。
   ⚠ `docs/T11b上線指令稿.md` 釘在 `0.1.1`,**不受本任務影響**;建議 T11b 的 D 段照原稿跑完再發 `0.2.0`,
   不要把兩件事綁進同一個窗口。
3. **24h 窗口是推理值**(Q3)—— 上線後若出現「同一事件隔天重複」,往上調 `PUSH_IDEMPOTENCY_WINDOW`。
4. **Q3 答覆的內部描述已不成立**(每日清理 → 不清理、隨訊息保留):對外語意不變,
   已在 `docs/契約對齊盤點_v3.3.md` Q3 下附註;下一次致 portal 的函件(T14b 前)一併告知。
5. 已知且接受:**同時**以一把**已過期**的 key 進來的兩個請求,兩者都會建立新訊息(重複,不是遺失)。
6. 來源後台**不提供改標籤**(刻意:寄件人名稱的變更不該是一個順手的按鈕);真有需要時另立任務。
7. `push_receipt` 與 `message` 同速成長,保留期隨 N3 一併決定(外鍵 CASCADE 已就位)。

---

## 版本歷史

| 版本 | 日期 | 修改人 | 摘要 |
|---|---|---|---|
| v1.1 | 2026-09-23 | Benny | T14a 完工:結果段。紅 25 failed → 綠全 repo **284 項**(文件層 70 + pytest 214 / 0 skipped,本機 PG 16.13);**突變檢查 36/36**(M23、M31 第一版紅在錯的地方,改寫成 M23′ / M31′ 後紅在預期的斷言上才算數)。🔴 四項發現:①**計畫段決定 5 那句「唯一擋得住」是錯的** —— id_token 同時被 `typ` 與「azp 是我方」兩道關擋住,單獨拿掉任一道都不會紅;跑突變前補上只有 `typ` 擋得住的兩格(計畫段以刪除線更正,不改寫原文);②FastAPI 在解析相依前就讀 body → 改成依賴認證的相依裡手動讀;③T10b 的 CSRF 列舉守門第一次自動涵蓋新表單(3 → 5 支);④`repo.py` 檔頭自 T07 起就是錯的。七項遺留(T14b 的 `X-User-Id` 待定點、`0.2.0` 發版注意、24h 是推理值、Q3 內部描述變更待告知 portal 等) |
| v1.0 | 2026-09-23 | Benny | T14a 計畫段(動工前寫)。T14 拆 T14a/T14b(第一條4,與 T11a/T11b 同型:阻塞條件不同);17 條驗收(6 條核定 + 11 條自加,每條附「壞掉時的症狀」);9 項設計決定,其中**決定 1 與 2026-08-24 答覆 Q3 的內部描述不同**(收據不做每日清理,因為它同時是稽核紀錄且本服務無排程器),對外語意不變,完工時回寫 Q3;誠實標註第二條3「經確認後才動工」本次的依據是一般性指示而非逐項確認 |
