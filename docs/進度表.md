# cats-inbox 進度表

**建立日期:** 2026-08-27 13:45
**最後更新:** 2026-09-01 12:10
**版本:** v1.8

> **這份文件回答一個問題:現在到哪裡了。** 一頁掃完。
> 逐任務的「做什麼、驗收標準」在 `docs/任務表.md`;逐任務的證據在 `docs/dev-logs/`。
> 🔴 **狀態欄由守門釘住**(`tests/test_docs.sh` 逐個 `Tnn` 比對任務表)——
> 這份文件複製了任務表的狀態,而複製會漂移。

---

## 1. 一句話現況

**`v0.1.1` 在 CATS VM 上跑著,而且 T11b 的 A、B 兩段全部結案**(2026-09-01)——
兩個容器 healthy、5 張表已建、A.3 三條容器紅線實測通過、**OIDC 三項驗證全過**
(gateway 解得到我方、新 client secret 可用、容器內 discovery 的 `iss` 比對為 `True`)。
🔴 **client secret 已輪替並實測驗證** —— 2026-08-28 那次曝光的值已失效。
🔴 **它還不能從統一入口到達** —— gateway 的 `/inbox/` 設定入庫了但**整段是註解**;
啟用只剩 **C 段**(portal PR,不需窗口)+ **D 段**(**約 10 分鐘的低峰窗口**)。

```
M0 ████████████ ✅   採用與計畫
M1 ████████████ ✅   骨架
M2 ████████████ ✅   SSO 接入
M3 ████████████ ✅   通知核心(公告先行)
M4 ██████░░░░░░ 🔵   掛上入口   ← T11a ✅ / T11b ⬜ / T12 ⬜
M5 ████░░░░░░░░ 🔵   系統通知   ← T13 ✅ / T14 ⬜(等第一張 S2S 憑證)
M6 ░░░░░░░░░░░░ ⬜   人對人      ← 等 DI-3 裁決(portal 承諾 M4 上線後啟動)
```

### 🔴 client secret 輪替(2026-09-01 結案)

2026-08-28 一次操作把 client secret 印在畫面上、進了對話紀錄
(**根因與處置見 dev-log `2026-09-01_T11b_A段B段實測`**)。已於 2026-09-01 完成輪替:

| 步 | 做了什麼 | 怎麼確認 |
|---|---|---|
| ① | Keycloak `kcadm` regenerate | — |
| ② | 重跑 portal `idp/bootstrap/client-cats-inbox.sh` | 交付檔重新鏡射 |
| ③ | 只換 `/opt/cats-inbox/.env` 的**那一行** | 腳本逐行比對:`只換了第 31 行,其餘 44 行一字未動` |
| ④ | `--force-recreate cats-inbox-api` | pg 顯示 `Up 3 days` = **沒被重建**;`alembic current` 仍 `0002 (head)` |
| ⑤ | 壞 code 打 token 端點 | `400 invalid_grant` = **新 secret 可用** |

🔴 **①②之間那一步差點被漏掉**:在 UI/CLI 輪替**不會**更新 portal 的交付檔,
而填值腳本是**從交付檔讀**的 —— 少了 ②,會把已廢止的舊值寫回去,**且不報任何錯**。
🔴 **③ 刻意不跑完整的 A-2**:那支腳本會重新產生 `POSTGRES_PASSWORD`,
而 postgres 映像的該變數**只在 `PGDATA` 是空的那一次生效** ——
`.env` 新密碼 / 資料庫舊密碼 → api 進重啟迴圈。**外流的是哪一個就換哪一個。**

---

## 2. 任務狀態(逐項)

| 里程碑 | 任務 | 狀態 |
|---|---|---|
| M0 | T00 憲法 + 計畫書 + 任務表 + 文件工具 + CI | ✅ |
| M1 | T01 服務骨架(health / Dockerfile / compose / CI) | ✅ |
| M1 | T02 回寫 portal 設計規劃(選址變更) | ✅ |
| M2 | T03 SSO client 申請 · T03b 契約 v3.3 逐條對齊 | ✅ ✅ |
| M2 | T04 OIDC 登入(PKCE + RS256/JWKS + 伺服器端續期) | ✅ |
| M2 | T05 首登建號 + 自動授 `reader`(DI-2 四條件) · **T05b CI 補 PG 15**(那 13 支從未在 CI 跑過)| ✅ ✅ |
| M2 | T06 登出雙向 + SLO front-channel 端點 | ✅ |
| M3 | T07 三張表 + migration · T08 讀取 API + 收件匣 UI | ✅ ✅ |
| M3 | T09 公告 · T09b 發布頁(表單 + CSRF) | ✅ ✅ |
| M3 | T10 安全紅線落地 · T10b CSRF 補齊 · T10c 映像 + 發版 SOP | ✅ ✅ ✅ |
| M3 | **T10d 映像帶入 alembic**(`0.1.0` 的映像跑不了 migration)| ✅ |
| M4 | **T11a gateway 路由設定入 portal** | ✅ |
| M4 | **T11b gateway 路由啟用(VM + 低峰套用)** | ⬜ |
| M4 | T12 上線冒煙 + landing 鈴鐺 | ⬜ |
| M5 | T13 §11.7 寫入型 S2S 申請 | ✅ |
| M5 | T14 推送 API | ⬜ |
| M6 | T15 DI-3 申請書 · T16 人對人 · T17 安全複驗 + 發版 | ⬜ ⬜ ⬜ |
| D | D01 架構說明 · D02 憲法第九條改版 · D03 首次發版實測 · D04 本表 · **D05 T11b 上線指令稿** | ✅ ✅ ✅ ✅ ✅ |

任務表共 **25 個 T 列**(含拆分出來的 T03b / **T05b** / T09b / T10b / T10c / T10d / T11a / T11b)
與 **5 個 D 列** → **完成 19 T + 5 D,未完成 6 T**。

<!-- 🔴 以下兩行由 tests/test_docs.sh 逐字比對任務表,改任一邊而忘了另一邊即紅 -->
**已完成:** T00 · T01 · T02 · T03 · T03b · T04 · T05 · T05b · T06 · T07 · T08 · T09 · T09b · T10 · T10b · T10c · T10d · T11a · T13 · D01 · D02 · D03 · D04 · D05
**未完成:** T11b · T12 · T14 · T15 · T16 · T17

---

## 3. 現在能用什麼(以及怎麼親眼看到)

✅ **已經在 VM 上跑著**(`cats-inbox-api` healthy、UID 1000、5 張表已建)。
⚠ 但**還不能從瀏覽器到達** —— 統一入口的 `/inbox/` 路由還是註解狀態(T11b 的 C/D 段);
現在只能用 `docker exec` 從容器內部看。

| 能力 | 怎麼看 |
|---|---|
| 健康檢查(不查 DB) | ✅ **VM 上實測**:`{'status':'ok','version':'0.1.1'}`(容器內) |
| OIDC 登入 / 登出 / SLO 端點 | ✅ **VM 上三項實測通過**(2026-09-01,B-6):壞 code 打 token 端點回 `400 invalid_grant`(=**secret 正確**,`invalid_client` 才是錯)、容器內 discovery `200` 且 `iss` 等於對外網址、gateway 容器解得到 `cats-inbox-api`。⚠ **真人端到端登入仍未做** —— 它需要 `/inbox/` 路由,留 D 段之後 |
| 收件匣頁(訊息 + 有效公告) | `/inbox/` —— 階段一**零人名**,只顯示 `source_app` 標籤與「平台公告」 |
| 公告發布頁(`announcer`) | `/inbox/announcements/new` —— 表單 + CSRF + 五種 400 的錯誤框 |
| 角色後台(`admin`) | 指派角色、`reader` 單獨停用、顯示名稱快取清除 |
| 容器映像 | `ghcr.io/fttp0165/cats-inbox-api:0.1.1` 已在 GHCR(**匿名可拉**;`tags/list` 回 `["0.1.0","0.1.1"]`,**無 `latest`**)。**首次部署用 `0.1.1`** —— `0.1.0` 跑不了 migration |

🔴 **還不能做的事**(誠實標示,不是缺漏):從統一入口進入(T11b)、入口未讀鈴鐺
(T12,portal 側)、各系統推送系統通知(T14)、人對人站內信(M6)。

---

## 4. 卡住的四件事,以及卡在誰身上

| # | 卡住的事 | 卡在誰身上 | 一旦解開能做什麼 |
|---|---|---|---|
| 1 | **一個低峰窗口(約 10 分鐘)** | **Benny** | ✅ **第 0 步 + A 段 + B 段全部完成**(2026-09-01)。剩 **C 段**(portal PR,**我做**,不需窗口)與 **D 段** —— 🔴 **只有 D 段需要窗口**。⚠ **B-4 已降為可選且已由 CI 覆蓋**(T05b,`ci #46` 實測 PG 15.8 通過)—— 不必對已部署的庫 drop 表 |
| 2 | **DI-3 顯示名稱裁決** | **portal**(承諾 M4 上線後與 rf-workorder #14 併案) | M6 全部(T15–T17)。⚠ 裁決前**不動工**,階段一刻意設計成零人名需求 |
| 3 | **第一張 S2S 憑證(DI-4 立案)** | **portal + 第一個來源系統** | T14 推送 API 接上第一個來源 |
| 4 | **portal 三問未覆**(登出導向頁結尾斜線、`source_app` 對應方式、`Idempotency-Key` 保留窗口) | **portal** | 前者影響 T12 冒煙、後兩者影響 T14 |

🔴 **第 1 項是唯一一個「只等一個人一個決定」的** —— 另外三項都要別的系統動起來。

🔴 **低峰窗口只需要約 10 分鐘,不是 2–3 小時。** 可貼指令稿:
**`docs/T11b上線指令稿.md`**(D05)。四段裡只有 D 段會動到正式站:

| 段 | 內容 | 需要窗口? | 估時 |
|---|---|---|---|
| 0 | 發 `v0.1.1`(🔴 `0.1.0` 的映像跑不了 migration,T10d)| ✅ **已完成 2026-08-28** | — |
| A | VM 建 `/opt/cats-inbox`、`.env`、併 secret、拉映像 | ✅ **已完成 2026-08-29** | — |
| B | 容器起來 ✅、跑 migration ✅、**PG 15 雙向演練 ✅ 改由 CI**(T05b)、OIDC 兩項 ✅、驗上游解析 ✅ | ❌ **不需要** —— gateway 上還沒有 `/inbox/` 路由,沒有流量會進來 | ✅ **已完成 2026-09-01** |
| C | portal repo:解註解 + 白名單 + staging `verify.sh` | ❌ | 30–40 分 |
| **D** | `ops/gateway-apply.sh` 套用 + 冒煙 | ✅ **只有這一段** | **約 10 分** |

⚠ 順序不可顛倒:白名單的入場券是 B 段的實測輸出
(portal 的判準是「**VM 上已實測存在**」)。
✅ **A 與 B 都做完了**(2026-09-01),兩段都沒有用到窗口 —— 當初把它們拆出來的判斷成立了。

---

## 5. 測試與發版現況

| 項目 | 數字 |
|---|---|
| 全 repo 測試 | **253 項** —— 文件層 **65** + pytest **188**(2026-09-01 實跑:起了本機 PG 之後 **188 passed / 0 skipped**)。文件層 +1 = 新守門「餵 heredoc 的 `docker exec` 必須帶 `-i`」|
| ⚠ 需真 PostgreSQL 的 migration 測試 | **13 項**(`test_migration.py` 3 + `test_schema.py` 10)。沒有真 PG 時它們是 **skipped**,而 skipped 與 passed 在輸出上長得很像 |
| CI | ✅ `ci #52` success(commit `d9d383d`,2026-09-01)—— postgres 的 log 為 `starting PostgreSQL **15.8**`,且 `ck_message_category` / `ck_announcement_audience` 兩個 constraint 被觸發,**證明那 13 支真的打到 PG 15**(不只是「沒有 skip」)。✅ 本輪起 `FATAL: role "root" does not exist` 的噪音**歸零**(健康檢查補了 `-U cats_inbox -d cats_inbox_mig`;⚠ 那只是不噴假錯誤,**不是把檢查變嚴**—— 真正擋住「role 沒建起來」的是下一步)。🔴 第一次讓那 13 支在 CI 上跑起來的是 `ci #46`(`4abf11c`):`188 passed`、**零 skipped**、`run_all.sh` 沒有印「⚠ 其中 N skipped」。🔴 **在那之前的 38 次全部都是 13 skipped**(CI 沒有 PostgreSQL,T05b 補上)。⚠ 1 warning = `actions/checkout@v4` 的 **Node 20 deprecation**(GitHub 平台的公告,不是本 repo 的問題)|
| 映像建置 | ✅ **兩次都 success** —— `#1` 79 秒、`#2` 60 秒,皆 8 步全過 |
| 已發版 | **v0.1.0** 與 **v0.1.1**,兩版的 Release 頁四段皆齊備(第八條1 ✅)。`v0.1.1` 發布於 2026-08-28 21:20;映像在 GHCR,`tags/list` 回 `["0.1.0","0.1.1"]` **無 `latest`** |
| 突變檢查(累計) | T09 17/17 · T10 19/19 · T09b 19/19 · T10b 13/13 · T10c 15/15 **全被抓到** |

**跑法(這一行決定那 13 項是 passed 還是 skipped):**

> 🖥️ **在哪執行:** WSL(Ubuntu)· 工作目錄 ~/Cats_inbox · 分支 `claude/cats-inbox-development-plan-ghwd9s`

```bash
eval "$(bash tests/pg_local.sh start)"
bash tests/run_all.sh
```

🔴 **這張表上面兩列曾經互相矛盾,而矛盾撐了很久(T05b,2026-08-28)。**
第一列寫「**183 passed / 0 skipped**」,第二列寫「**沒有真 PG 時那 13 項是 skipped**」——
第一列成立的條件正是第二列講的那一行指令,而**第一列沒有把條件寫出來**。

🔴 **真正被漏掉的不是數字,是一個問題沒有人問:CI 屬於哪一種?**
第二列老實寫著「沒有真 PG 時會 skip」,而**沒有任何一列去問 CI 有沒有真 PG**。
答案是沒有 —— `.github/workflows/ci.yml` 從 T01 起就沒有 `services:` 區塊,
於是那 13 支(schema 與 migration 的測試,含共通紅線要求的 **up→down→up 雙向演練**)
**在 38 次 CI 上一次都沒跑過**,而第三條2 的名字就叫「納入 CI 防止回歸」。
⚠ **skip 不是失敗,綠燈仍然是綠燈** —— 這是它撐了那麼久的全部原因。

---

## 6. 🔴 已知但尚未驗證的事(原三件,**只剩一件**)

這一節刻意獨立 —— 它們**不是待辦事項**,是「我們相信但沒看過」的東西。

🔴 **只剩第 1 項,而它正是 C 段要驗的那一件** —— staging 的 `probe /inbox/abc` 是本專案第一次真的把 nginx 前綴行為跑起來看。

| # | 相信什麼 | 為什麼還沒驗 | 錯了會怎樣 |
|---|---|---|---|
| 1 | `proxy_pass http://cats-inbox-api:8080;`(**不加尾斜線**)會原樣轉發 `/inbox/…` | 開發機**沒有 nginx** | **全站 404,而 gateway 這一層完全正常**。由**指令稿 C-3** 的 staging `probe` 驗 |
| 2 | ~~migration 在 **PG 15** 上 up→down→up 通過~~ → ✅ **2026-08-29 由 CI 實測通過** | `ci #46`(commit `4abf11c`)跑在 `postgres:15.8-alpine`(**與 VM 同一個 tag**),`test_migration_up_down_up` 在其中,`188 passed / 0 skipped`。🔴 **而且從此每次 push 都跑** —— 不是一次性的演練 | 平台紅線是 PG 15。⚠ **指令稿 B-4 因此降為 🟡 可選**:它是對**已部署的庫** drop 5 張表,只跑一次且要有人記得,而多覆蓋的僅「這一個實例的 downgrade」|
| 3 | ~~容器在真的 Docker 上啟動~~ ✅ · ~~容器內 discovery 連得到 Keycloak~~ → ✅ **2026-09-01 實測通過** | `docker exec` 進容器打 discovery 回 200,且 `iss` == `https://catsapp.sporton.com.tw/auth/realms/sporton`(**比對為 `True`**);同批的壞 code 換 token 回 `400 invalid_grant` = **secret 正確** | `INBOX_OIDC_INTERNAL_BASE` 填錯會組出 `/realms/sporton/realms/sporton` → discovery 404,而**離線測試全綠,只在第一個真人登入時出現** —— 這個坑已經確定沒踩到 |

---

## 7. 已知的功能缺口(記錄在案,不是漏掉)

| # | 缺口 | 現況 |
|---|---|---|
| 1 | 收件匣列表**沒有分頁**(`limit=50`) | 記於 `docs/架構說明.md` §5.2;上線後看實際量再議 |
| 2 | 未讀鈴鐺**不數公告** | `messages/unread-count` 的語意刻意沒改;🔴 **T12 必須明講要不要數**,否則發出去的公告在入口頁**零信號**而每一層都回 200 |
| 3 | CSRF 失敗回的是**原始 JSON**,不是錯誤頁 | 已記錄為建議,未做 |
| 4 | `audience` 的「群組」做不到 | 需 `groups` claim 而我方 client 刻意未申請;已以 CHECK 擋在 `all` |

---

## 8. 上位契約對齊現況

| 契約 | 已對齊版本 |
|---|---|
| 帳號系統接入契約(SSO/OIDC) | **v3.3** —— ✅ 逐條對齊完成(T03b,14 版比對) |
| App 服務對齊指南(gateway) | **v1.3** |
| 站內信通知中心設計規劃(功能上游) | **v1.6** |
| portal 平台紅線(`CLAUDE.md`) | **v1.21**(本專案已登記於其系統清單,`/inbox/`) |
| S2S 章節 | ✅ §11.7 寫入型**已獲原則核可**;⏳ 第一張憑證待 DI-4 立案 |

---

## 版本歷史

| 版本 | 日期 | 修改人 | 摘要 |
|---|---|---|---|
| v1.8 | 2026-09-01 | Benny | **§5 的 CI 那一列更新到 `ci #52`**(`d9d383d`)。本輪的證據不只是綠燈:postgres 的 log 顯示 **15.8**,且 `ck_message_category` / `ck_announcement_audience` 兩個 constraint 被觸發 —— **那 13 支真的打到 PG 15**,不只是「沒有 skip」。✅ 並記下 `FATAL: role "root" does not exist` 的噪音**歸零**(v1.6 記的那個健康檢查缺 `-U` 的問題已修);⚠ 同時保留誠實標註:**補 `-U` 只是不噴假錯誤,不是把檢查變嚴**。🔴 保留「第一次是 `ci #46`」那句 —— 它記的是那 13 支從 skip 變成真的跑的那一刻 |
| v1.7 | 2026-09-01 | Benny | **T11b 的 A、B 兩段全部結案後回寫**。①§1 改寫並新增〈**client secret 輪替**〉一節(五步 + 每步的確認方式)——🔴 記下兩個差點踩到的坑:**輪替不會更新 portal 的交付檔**(而填值腳本是從交付檔讀的,少了那一步會把已廢止的舊值寫回去**且不報錯**),以及**已部署後不可跑完整的 A-2**(它會重新產生 `POSTGRES_PASSWORD`,而 postgres 映像的該變數只在 `PGDATA` 是空的那一次生效 → api 重啟迴圈)。**外流的是哪一個就換哪一個。** ②§3 的 OIDC 那一列由「需三個必填值」改為**三項實測通過**(`400 invalid_grant` / discovery `iss` 比對 `True` / gateway 解得到),並誠實標**真人端到端登入仍未做**(要 `/inbox/` 路由)。③§4 第 1 列與分段表的 B 列改為 ✅,並記下「**A 與 B 都沒用到窗口**——當初把它們拆出來的判斷成立了」。④§5 測試數字 252 → **253**(文件層 64 → **65**,新守門:餵 heredoc 的 `docker exec` 必須帶 `-i`)。⑤§6 第 3 項的 discovery ✅ 結掉 → **原三件只剩一件**,而剩的那件正是 C 段要驗的 |
| v1.6 | 2026-08-29 | Benny | **T05b 的 CI 實跑結果回寫 —— §6 第 2 項結掉**。`ci #46`(commit `4abf11c`)success,而**判準不是綠燈**(綠燈在 T05b 之前也是綠的):`188 passed`、**零 skipped**、`run_all.sh` **沒有**印「⚠ 其中 N skipped」那一行,postgres 的 log 為 `starting PostgreSQL **15.8**`。🔴 **最硬的一項證據在 postgres 自己的 log 裡** —— `ck_message_category` 與 `ck_announcement_audience` 兩個 check constraint 被觸發,那是 `test_schema.py` 的反向斷言**真的打到 PG 15**,不只是「沒有 skip」。→ §6 第 2 項(PG 15 up→down→up)✅ **結掉,而且從此每次 push 都跑**;節名由「三件事」改為「原三件,已結掉兩件」。⚠ 同一份 log 也暴露**我這一輪剛寫的健康檢查有一個洞**:`--health-cmd pg_isready` 沒帶 `-U`,postgres 每 10 秒噴一次 `FATAL: role "root" does not exist`(共 5 次)**而健康檢查照樣回綠** —— `pg_isready` 只判斷「伺服器有沒有回應」,回一個 FATAL 也算有回應。已補 `-U cats_inbox -d cats_inbox_mig`,並在 `ci.yml` **誠實註明帶了 `-U` 也不會驗到 role 真的存在**(真正擋住那件事的是下一步);**第六次「粒度比它宣稱保護的性質粗」,而這次在我自己剛寫的程式碼裡** |
| v1.5 | 2026-08-29 | Benny | 🔴 **T05b:那 13 支 schema/migration 測試從未在 CI 上跑過**(本來只是要修 §5 一個過期的數字,跑測試取真數字時看到的是 `172 passed, **13 skipped**`)。①§5 第一列 `247 項 / 183 passed / **0 skipped**` → **252 項 / 188**,並寫出「0 skipped」**成立的條件**(要先 `pg_local.sh start`)—— 🔴 **本表上下兩列原本互相矛盾**:第二列老實寫著「沒有真 PG 時那 13 項是 skipped」,而第一列直接宣稱 0 skipped。②🔴 **真正被漏掉的不是數字,是一個沒有人問的問題:CI 屬於哪一種?** 答案是「沒有真 PG」那一種 —— `ci.yml` 從 T01 起就沒有 `services:` 區塊,那 13 支(含共通紅線要求的 **up→down→up 雙向演練**)**在 38 次 CI 上一次都沒跑過**,而第三條2 的名字就叫「納入 CI 防止回歸」。⚠ **skip 不是失敗,綠燈仍然是綠燈。** ③§6 第 2 項改寫:CI 已補 **`postgres:15.8-alpine`(與 VM 同一個 tag)**,PG 15 的雙向演練從此**每次 push 都跑**;⚠ **指令稿 B-4 因此降為可選** —— 它是對**已部署的庫** drop 5 張表,而 CI 覆蓋同一個性質且永久生效。④§4 兩處同步(第 1 列與 B 段那一列)|
| v1.4 | 2026-08-29 | Benny | **T11b 的 A 段與 B-1/B-3 完成後回寫 —— `v0.1.1` 真的在 VM 上跑起來了**。①§1 改寫:兩個容器 healthy、**5 張表已建**(`alembic current` = `0002 (head)`)、**A.3 三條容器紅線實測通過**(api 在 `cats-edge`、pg **只在** `inbox-internal`、pg Ports `{"5432/tcp":null}` 未 publish)、UID 1000、health 回 `version 0.1.1`。②§3 導言由「只能在開發機看到」改為「已經在 VM 上跑著,但還不能從瀏覽器到達」—— 🔴 那是兩件不同的事。③§6 第 3 項的「容器在真的 Docker 上啟動」✅ **結掉**(discovery 那半仍 ⏳);第 2 項標明 **postgres:15.8-alpine 且 `upgrade head` 通過**,而 **down→up 的雙向演練仍未做**(指令稿 B-4)。④§4 第 1 列改為「只剩 B-4/B-6/C 段,而**只有 D 段需要窗口**」|
| v1.3 | 2026-08-28 | Benny | **`v0.1.1` 的 Release 頁已建,第 0 步整段完成**。①§5「已發版」那列由「🔴 Release 頁尚未建」改為**兩版四段皆齊備**(第八條1 ✅;`v0.1.1` 發布於 2026-08-28 21:20)—— 🔴 **那句話昨天為真,今天是假的**,而它正是本表最容易變假的一種句子(進度表的價值全在它是現況)。②CI 那列更新為 `ci #38`(46 秒,`d800c16`),並註明那 1 warning 是 `actions/checkout@v4` 的 **Node 20 deprecation** —— **GitHub 平台的公告,不是本 repo 的問題**,免得下次有人把它當缺陷追。③§4 的第 0 步標 ✅,並明寫**下一步是 A 段而它不需要窗口** |
| v1.2 | 2026-08-27 | Benny | **`v0.1.1` 已發版後回寫**:tag 已推、`release-image #2` success(60 秒)、映像已上 GHCR(`tags/list` 回 `["0.1.0","0.1.1"]`,**無 `latest`** —— 離開 GitHub 問 registry 驗的)。§1/§3/§5 的版本號由 `0.1.0` 改為 `0.1.1`。🔴 **誠實標註一個未完成狀態**:`v0.1.1` 的 **Release 頁尚未建**(API 查回 404)—— **裸 tag 的狀態持續著**,而第八條1 明訂不得只推裸 tag;內容已備妥在指令稿第 0 步。⚠ 這件事的根因已回寫到指令稿 v1.3 與發版SOP v1.4:那一步原本寫成一行 `⚠` 提醒,而**提醒不是步驟** —— 照著清單做的人會做完編號的最後一項就停 |
| v1.1 | 2026-08-27 | Benny | **T10d / D05 完成後回寫**。①逐任務表新增 **T10d**(映像帶入 alembic —— 🔴 `0.1.0` 的映像**跑不了 migration**,而 Release 頁明寫要跑)與 **D05**(T11b 上線指令稿);計數 23→**24 個 T 列**、4→**5 個 D 列**,完成 21→**23**。②🔴 **§4 改寫:低峰窗口從「2–3 小時」縮到「約 10 分鐘」** —— 指令稿逐步寫下來之後發現四段裡**只有 D 段(`gateway-apply.sh`)會動到正式站**,A/B/C 都不需要窗口(gateway 上還沒有 `/inbox/` 路由,沒有流量會進來);⚠ 順序仍不可顛倒,白名單的入場券是 B 段的實測輸出。💡 並標明 **A 與 B 今天就能做**。③§6 的第 2、3 項標上指令稿裡對應的步驟編號(B-4 補 PG 15 演練、B-6 驗容器內 discovery)|
| v1.0 | 2026-08-27 | Benny | 初版(D04,Benny 指示「寫目前進度表」)。八節:一句話現況 + 里程碑進度條、逐任務狀態(14 T + 4 D 完成 / 5 T 未動工)、**現在能用什麼與怎麼親眼看到**、**卡住的四件事與卡在誰身上**(🔴 只有第 1 項是「等一個人一個決定」,另三項要別的系統動起來)、測試與發版現況(234 項全綠、`release-image #1` 與 `ci #30` 雙綠、v0.1.0 已發版)、🔴 **已知但尚未驗證的三件事**(獨立成節——它們不是待辦,是「相信但沒看過」:`proxy_pass` 不加尾斜線、PG 15 演練、容器內 discovery,三者錯了的症狀都是**離線測試全綠而只在正式環境出現**)、已知功能缺口四項、上位契約對齊現況。🔴 **狀態欄由 `tests/test_docs.sh` 逐個 `Tnn` 比對任務表釘住** —— 本表複製了任務表的狀態,而複製會漂移,且本表是唯一一份設計上要給人掃一眼就信的文件 |
