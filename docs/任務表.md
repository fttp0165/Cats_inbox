# cats-inbox 任務表

**建立日期:** 2026-08-15 08:55
**最後更新:** 2026-08-26 20:05
**版本:** v1.20

> 依 `docs/開發計畫書.md` §4 展開;狀態 ⬜ 未動工 / 🔵 進行中 / ✅ 完成(憲法第一條2)。
> **每個任務的紅測試規格見 `docs/TDD測試計畫表.md`**(同一組 Tnn 編號:本表管「做什麼」,該表管「怎麼證明做到了」)。
> 影響分級(對現有服務/資料):🟢 無影響 / 🟡 需低峰或協調 / 🔴 不可做。
> 每任務動工前依第二條在 `docs/dev-logs/` 建日誌(目標/影響範圍/驗收標準/回滾方式),完工補結果。

---

## M0 採用與計畫

| 編號 | 任務 | 驗收標準(摘要) | 估時 | 影響 | 依賴 | 狀態 |
|---|---|---|---|---|---|---|
| T00 | 憲法 + 開發計畫書 + 任務表 + 文件工具 + CI | `tests/test_docs.sh` 全綠;md+HTML 並存且 `--check` 同步;憲法附錄 A 特化完成 | 4 h | 🟢 純文件 | — | ✅ 2026-08-15 |

## M1 骨架

| 編號 | 任務 | 驗收標準(摘要) | 估時 | 影響 | 依賴 | 狀態 |
|---|---|---|---|---|---|---|
| T01 | 服務骨架:FastAPI `/inbox/health`(不查 DB)、Dockerfile(non-root、二段建置)、compose(`cats-edge` external 治本寫法 + `cats-inbox-pg` 僅內網)、`.env.example`、`test_skeleton.py`+`test_app.py` 紅→綠、CI 跑 run_all | ✅ 23 項 pytest 全綠(骨架 19 + 應用 4);`docker compose config` 解析通過;DB 指向壞主機時 `/inbox/health` 仍 200(證明不查 DB);pg 無 ports、不在 cats-edge;`.env.example` 涵蓋 config.py 全部變數。⚠ **容器實際啟動未驗**(本環境無 docker daemon),留 T11 於 VM 補 | 5–7 h | 🟢 | — | ✅ 2026-08-15 |
| T02 | 回寫 portal《站內信通知中心設計規劃》:選址由「掛 compliance」改為獨立服務 cats-inbox,升版並留版本歷史(DI-1) | ✅ 該文件 **v1.3 → v1.6**(⚠ 原編 v1.4 與 main 既有 v1.4 撞號,合併時改編)(第 2 章改寫 + §2.1 理由;功能設計未動);連帶修三處已知錯誤(刪 X-Api-Key、proxy 目標、刪已不成立的 CORS 敘述)、N4 結案、新增 N6;portal `CLAUDE.md` **v1.19** 系統清單登記 cats-inbox(與 SENSE 並存);待辦清單 **v3.6** 掛入五項(DEC-16/17/18、EXT-11/12);md+HTML 以 portal 自家 `tools/md2html.py` 同步;其文件測試 2/48/63 全綠。已開 **portal PR #98**,CI 綠 | 1–2 h | 🟢 純文件(portal repo) | DI-1(已定) | ✅ 2026-08-15 |

## M2 SSO 接入

| 編號 | 任務 | 驗收標準(摘要) | 估時 | 影響 | 依賴 | 狀態 |
|---|---|---|---|---|---|---|
| T03 | 依契約 §5 申請 client:`cats-inbox`、confidential+PKCE、redirect URI `https://catsapp.sporton.com.tw/inbox/oidc/callback/`、**session 形態聲明=cookie/伺服器 session**、需要 claims:`name`;隨函附 DI-2 開通政策偏離申請 | ✅ **申請書已送出**(`docs/SSO接入申請.md` → **v1.1**,已對齊契約 v3.3;副本依 portal 第四條7 命名為 `cats-portal DOCS/cats-inbox_to_cats-portal_申請_SSO接入與開通政策偏離_20260817-2300.md`)。✅ **2026-08-18 portal 回覆**:受理、DI-2 **核可**(四條件)、寫入型 S2S **原則核可**、DI-3 不現在裁決;契約 §6/§9/§11.8 已登記;client 腳本 `idp/bootstrap/client-cats-inbox.sh` 已備妥(通過其 157 項守門)。✅ **client 已於 2026-08-18 02:10 在 VM 建立**(`a51d9925-fdec-48ca-be2d-32f5dbe9d7fb`),回讀驗證四組全過(confidential+PKCE S256+禁 implicit/password/service account、redirect URI 無萬用字元、post-logout 兩值+front-channel URL 無結尾斜線、profile 在位且 **email scope 已移除**);交付檔 `idp/.env.keycloak.cats-inbox`(600)已產生。⏳ **secret 尚未進本服務 `.env`**——`/opt/cats-inbox` 尚不存在(T11 部署時併入,portal EXT-13)。⏳ 我方待回覆三問:登出實際導向頁+結尾斜線、`source_app` 對應方式、`Idempotency-Key` 保留窗口 | 1 h(+portal 作業) | 🟢 | T01 | ✅ **2026-08-18 獲覆** |
| **T03b** | 🔴 **逐條對齊 SSO 契約 v2.7 → v3.3**(送件時才發現差了 §11 整章)。範圍:§11 S2S 全章、§4.2b 通知用途 email、§9/§10.6 變動、§10.3a 端點結尾斜線、§3.1 claims 變動 | 產出對齊盤點文件(md+HTML);A.2 標為「逐條已對齊」;**凡影響 T04–T06 驗收者一併回寫任務表與 TDD 計畫表** | 2–3 h | 🟢 純文件 | T03 ✅ | ✅ **2026-08-18**(`docs/契約對齊盤點_v3.3.md`) |
| T04 | OIDC 登入(⚠ 動工前先做 T03b;**client 已核發,本機開發可用真 issuer + 假 token,正式端到端要等 T11 部署後拿到 secret**):Authorization Code+PKCE、state/nonce、RS256/JWKS(1h 快取、kid 輪替)、驗 `iss`/`aud`/`exp`±30s、容器內走 `OIDC_INTERNAL_BASE` 而 `iss` 不改、**伺服器端 refresh_token 主動續期**(§3.3 陷阱) | auth 測試綠:竄改簽章/過期/錯 aud→401;登入後過 300s 頁面仍有效(續期證據);測試替身含過期案例**且帶真實 IdP 會給的 claims(含 `at_hash`)**。🔴 **T03b 新增兩項**:①**登入前印出實際要送出的 `redirect_uri`,與 client 登記值逐字比對**(契約 v2.14:PLM 因 `reverse()` 回後註冊者而首登即 mismatch,錯誤停在 Keycloak 頁面、app 的 log 是空的);②**用壞 code 打 token 端點驗 secret**——回 `400 invalid_grant` 即 secret 正確、`invalid_client` 即錯(契約 v2.17;**不需真人登入、不需 gateway 路由**)。**實測**:12 支 auth 測試全綠(合計 76 項);redirect_uri 逐字相符已由 log 印出並比對;推進 301 秒後 `/inbox/me` 仍 200 且 log 有 `oidc_refreshed`;9 項突變檢查 8 項被測試抓到(第 9 項見 dev-log)。⏳ **真人端到端、壞 code 驗 secret、容器內 discovery 三項留 T11**(尚無 secret) | 6–8 h | 🟢 | T03 | ✅ **2026-08-21** |
| T05 | 首登建號+**自動授 `reader`**(DI-2 已核可,僅自我範圍讀取)+待開通+角色後台:user 表只存 `sub`+角色;待開通頁顯示本人 `sub`;bootstrap 管理員清單**每次登入比對**(idempotent、disabled 不復活);admin 派角色 UI;`display_name` L1 快取(僅本人 token 寫入、僅管理後台顯示、附整批+單筆清除工具) | 首登 DB 只多一列(無 email/密碼欄);**首登即有 `reader`**、可讀自己收件匣;**無 `sender`/`announcer` 一律 403(反向測試,DI-2 條件 C3)**;`reader` 可單獨停用(C2);清單內帳號再登入自動升級;快取清除工具實測 。**實測**:13 支測試綠(全 repo 101 項);10 項突變檢查**全被抓到**;migration up→down→up 在**真的 PostgreSQL** 上通過。🔴 **`app_user` 與 Alembic 由 T07 提前到本任務**(理由見 dev-log:記憶體版讓四條驗收失去意義)。⚠ **本機只有 PG16.13**(無 PG15、無 docker daemon)——**PG15 演練留 T11 於 VM 補**,不得視為已符合 PG15 紅線 | 5–7 h | 🟡 **首次建表** | T04 | ✅ **2026-08-24** |
| T06 | 登出雙向:自發登出(先清自己 session → 導 IdP end_session);SLO front-channel 端點 `GET /inbox/oidc/frontchannel-logout`(免認證、冪等、只刪 cookie、204、`iss`/`sid` 可有可無);端點上線後知會 portal 註冊 | 冒煙:入口登出→換帳號→進 inbox 不得仍是前一人(**測前清 cookie/無痕**,§10.7);重複呼叫端點皆 204。🔴 **T03b 新增**:route 字串必須與 client 登記值**逐字相同**=`/inbox/oidc/frontchannel-logout`(**無結尾斜線**,§10.3a)——Keycloak 拿它去**呼叫**而非比對,差一個字元的症狀是**端點零呼叫**,與「設定沒套用」完全相同(PLM 2026-08-14 前例)。**實測**:9 支登出測試全綠(合計 85 項);6 項突變檢查全被抓到;post-logout 改用 **`/inbox/logged-out/`**(已在 client 登記值內,portal 零改動)並已實作該頁。⏳ **真人冒煙(換帳號驗證)留 T11** ——§7 明訂測前須清 cookie/無痕,而現無 gateway 路由 | 2–3 h | 🟢 | T04 | ✅ **2026-08-24** |

## M3 通知核心(公告先行)

| 編號 | 任務 | 驗收標準(摘要) | 估時 | 影響 | 依賴 | 狀態 |
|---|---|---|---|---|---|---|
| T07 | 資料模型與 migration:**`message`/`announcement`/`announcement_read` 三張表**(Alembic,每條附 backward)。⚠ **`app_user` 與 Alembic 基礎設施已於 T05 提前完成**(T05 有四條驗收是資料庫斷言,記憶體版讓它們失去意義);本任務接在 revision `0001` 之後 | 本機 up→down→up 演練通過;schema 無 email/姓名/密碼欄。🔴 **PG15 的演練仍待 T11**(T05 只在 PG16 演練過) 。**實測**:10 支測試綠(全 repo 111 項);9 項突變檢查**全被抓到**;`0002` up→down(→`0001`)→up 通過且**降級不動 T05 的兩張表**。🔴 **補了 T05 的盲點**:`test_models_match_migration_schema` 在同一個 PG 上開兩個庫,一邊跑 migration、一邊跑 `create_all()`,逐表逐欄比對。⚠ 欄位名照上游 §4 逐字沿用(含 `announcement.title` vs `message.subject`);🔴 **`audience` 的「群組」做不到**——需 `groups` claim 而我方 client 刻意未申請,已以 CHECK 擋在 `all` | 2–3 h(原 3–4 h,少一張表) | 🟡 第二條 migration | T05 | ✅ **2026-08-24** |
| T08 | 讀取 API + 收件匣 UI:列表(unread 過濾)、`unread-count`、標已讀(冪等)、收件匣頁(base path `/inbox/`、Bootstrap 5 本地託管、light) | deny-by-default 測試:讀他人訊息 403;`sender_sub` 不信前端;UI 零外部 CDN;階段一 UI **零人名**(source_app 標籤)。**實測**:14 支測試綠(全 repo 125 項);9 項突變檢查全被抓到;Bootstrap 5.3.8 **自 portal-admin 複製**(本環境無法對外取得,雜湊與其逐字相同),含 LICENSE/PROVENANCE/sha256。🔴 零人名那條**先在 L1 快取寫入姓名再斷言不出現**(不先寫入的話快取為空時必然通過=假綠),且**API 回應面也一併斷言**(突變檢查補上——原本只驗 HTML,而往回應加姓名欄位測試照樣綠) | 6–8 h | 🟢 | T05、T07 | ✅ **2026-08-25** |
| T09 | 公告:發布(`announcer` 角色)、有效期 starts/ends、`announcements/active`、公告已讀(AnnouncementRead)。🔴 **範圍擴充(第一條4)**:併入**收件匣頁顯示有效公告** ——原敘述只有三個 API,而 M4 的名字是「公告中心上線」,其下 T11(路由)T12(冒煙+鈴鐺)**都不含公告的畫面**;照原表走完 M4,公告上線後沒有任何畫面顯示它,而發布 API 回 201、`active` 也回得出來 —— **零錯誤訊息的功能缺口**。寫的那一面(發布表單)另立 T09b |非 announcer 發布 **403 且零列寫入**(只驗狀態碼的話「先寫再回 403」照樣綠);過期公告不出現在 active;**未來的公告也不得出現**;已讀逐人記錄不複製列且 `read_at` 不被覆寫;甲讀過不得讓乙變已讀;`ends_at<=starts_at` 與**不帶時區**的時間一律 400;回應零人名零 `author_sub`。**實測**:21 支測試綠(全 repo **151 項**:文件層 45 + pytest 106);**17 項突變檢查全被抓到**(第一輪有 1 項顯示未抓到,查為 **sed 沒真的改到語意**,不是測試有洞——改對後即紅)。🔴 順手修掉一個跨 T08 的問題:回應的時間欄位在 **SQLite 上不帶時區**而 PG 上帶,而我方寫入端正是拒收不帶時區的值——已統一走 `iso_utc()`,輸出與輸入對稱 | 3–4 h | 🟢 | T08 | ✅ **2026-08-25** |
| **T09b** | 公告發布頁(給 `announcer` 的表單):標題/內容/有效期、**CSRF**、錯誤訊息對應五種 400、收件匣頁的入口連結。🔴 **本列是 T09 動工時發現的缺口,不是原計畫**——沒有它,上線後 `announcer` 只能用 curl 發公告 | `announcer` 200 / `reader` **403** / 未登入 **401**;**CSRF**:缺、亂給、**用別的 session 的 token** 一律 403 **且零列寫入**;🔴 **時區**:表單填 `2026-08-30T02:00`(台北)→ DB 存 `2026-08-29T18:00Z`(斷言**實際的 UTC 值**,只斷言「成功」的話差 8 小時也會通過);五種 400 在**錯誤框內**有具體訊息**且已填內容仍在**;成功 **303** 導回收件匣;入口連結**只給有能力的人看**(反向也測)。**實測**:17 支測試綠(全 repo **209 項**);**19 項突變檢查全被抓到**——⚠ 第一輪有 **3 項沒抓到,而三個都是測試斷言自己寫錯**(斷言的對象裡本來就含有要找的字串),已修 | 2–3 h | 🟢 | T09 ✅ | ✅ **2026-08-25** |
| T10 | 安全紅線落地:body 純文字+輸出跳脫、`action_url` 同站白名單(**寫入側與輸出側各驗一次**)、內容不進 log、嚴格 CSP+nonce | 反例 **14 則**全 400:外部網址、`http` 降級、`javascript:`(含大小寫變形)、`data:`、`//` 與 **`/\`** 協定相對、**前綴冒充 `...com.tw.evil.tld`**、**userinfo 冒充 `...com.tw@evil.tld`**、無 scheme、NUL、換行、超長;log 抽查無 subject/body 且每行為單行 JSON;**每個回 HTML 的路由都有嚴格 CSP**、頁內 `<style>` 帶**該次回應**的 nonce、兩次請求 nonce 不同。**實測**:41 支測試綠(全 repo **192 項**:文件層 45 + pytest 147);**19 項突變檢查全被抓到**。🔴 **一項發現**:嚴格 CSP **連 `style="…"` 屬性也擋**(nonce 只對元素有效),原有 5 個行內樣式屬性全部改 class —— 不改的症狀是**版面走鐘而伺服器零錯誤**;`action_url` 的關卡設在 **`repo.create_message()`** 並以 AST 釘住「`app/` 底下唯一寫入路徑」(它的端點是 T14 才有的,設在端點上等於在還不存在的地方設關卡) | 2–3 h | 🟢 | T08 | ✅ **2026-08-25** |

| **T10b** | 🔴 **CSRF 補到所有 POST 表單**,並改成**列舉式守門**(比照 T10 的 CSP:逐處加會忘記,而忘記沒有症狀)。範圍:角色後台的「清除顯示名稱」與「指派角色」——**它們從 T05 就沒有 CSRF**(T09b 發現)| 列舉所有收表單的 POST 路由:無 token → **403 且零副作用**、**有 token → 不得是 403**(否則後台完全不能用);錯誤碼與「沒權限」分開(`csrf_failed`);失敗留 log 但**不記表單內容**;後台每個 `<form>` 都要有值為 **64 字元 HMAC** 的 hidden input。**實測**:7 支測試綠(全 repo **216 項**);**13 項突變檢查全被抓到**。🔴 **三項發現**:①**T10 的「列舉所有 HTML 路由」守門實際上只驗到自動產生的文件頁** ——FastAPI 的 `_IncludedRouter` 讓 `app.routes` 看不到真正的路由,而「至少 3 個」剛好被 3 個文件頁滿足,於是一直全綠而**四個真頁面一個都沒驗到**(已建 `tests/conftest.py::iter_routes()` 統一處理;`_IncludedRouter` **T06 已記過一次**);②列舉式守門會打到 `/inbox/logout` 而它**清掉 session**,後面的頁面全變 401 被靜靜跳過(改成每支之前重新登入);③**加這道保護時把 401/403 的分界弄壞了**(沒有 session 時回 403),抓到它的是 **T09b 留下的測試** | 1–2 h | 🟢 | T09b ✅ | ✅ **2026-08-25** |

| **T10c** | 🔴 **映像建置與推送 + 發版 SOP**。`docker-compose.yml` 引用 `ghcr.io/fttp0165/cats-inbox-api:<版本>`,而**那個映像不存在、沒有任何 workflow 建它、沒有任何任務提到它** —— 每份文件都對、每個任務也都完成了,而把它們接起來的那一步不在任何人的清單上。⚠ 症狀會在 **T11 的低峰窗口**當場出現(`manifest unknown`)| workflow **只在推 tag 時**觸發(+`workflow_dispatch` 供補建);**不推 `latest`**;`permissions` 最小化;**三道一致性檢查各自會讓 workflow 失敗**(git tag == `__version__` == compose 的 tag、映像名與 compose 相符);**推之前先把映像跑起來**驗 health 的版本與 non-root;`docs/發版SOP.md`+`.html` 含第八條的 title 格式與四段 content。**實測**:9 支測試綠(全 repo **231 項**,文件層 45 → **51**);**15 項突變檢查全被抓到**;兩個 `sed` 與每個 `run:` 區塊已在本機實測。🔴 **誠實標註(寫於 T10c 當時)**:workflow **從未在真的 Actions 上跑過**(本機無 docker daemon、無 GHCR 憑證),映像**當時仍不存在** —— 第一次發版就是這份 SOP 的第一次演練。✅ **已於 2026-08-26 首次真的執行並成功**(`release-image #1`,79 秒,映像已推 `0.1.0`)—— 回寫見 **D03**。⚠ 順手把 `test_docs.sh` 的**三份手寫清單改成自 `TARGETS` 自動產生**(D01 記過:新文件不登記進清單,`--check` 會全綠)| 2–3 h | 🟢 | T01 | ✅ **2026-08-25** |

## M4 掛上入口(公告中心上線)

| 編號 | 任務 | 驗收標準(摘要) | 估時 | 影響 | 依賴 | 狀態 |
|---|---|---|---|---|---|---|
| T11 | gateway 路由:cats-portal 提 PR(`/inbox/` → `cats-inbox-api:8080` + proxy_headers);VM 部署;低峰套用 | 容器在 `cats-edge` 短名可解析;`nginx -t` 過;`curl -skI https://catsapp.sporton.com.tw/inbox/health` 200;既有路由(`/plm/` `/TMP_GEN/` 等)zero-diff;可秒級回滾 | 2–3 h(+portal 側 1–2 h) | 🟡 gateway reload | T01–T10 | ⬜ |
| T12 | 上線冒煙 + landing 鈴鐺:跑契約 §7 全清單(未完成項如實打叉);portal-landing 加鈴鐺(30s 輪詢 `unread-count`,portal 側施工) | §7 清單結果寫入 dev-log;鈴鐺在 IdP/inbox 不可用時不擋首頁(漸進增強);上線公告發出。🔴 **T10 新增**:本服務的 CSP 是 `frame-ancestors 'none'`(**不可被 iframe 嵌入**)。入口若改成嵌入而非連結,必須先改那一行 —— 否則 iframe 是一片空白**而零錯誤訊息**。🔴 **T09 新增**:**鈴鐺要不要把公告算進未讀,必須明講**——現行 `messages/unread-count` 的名字就叫 `messages`,T09 刻意沒有改它的語意;不處理的話**發出去的公告在入口頁零信號**,而每一層都回 200 | 3–4 h(+portal 側 2–3 h) | 🟡 landing 改版 | T11 | ⬜ |

## M5 系統通知開通

| 編號 | 任務 | 驗收標準(摘要) | 估時 | 影響 | 依賴 | 狀態 |
|---|---|---|---|---|---|---|
| T13 | ~~S2S 章節送件~~ → **改為依 §11.7 申請寫入型**(章節早於契約 v3.0 定稿,原任務前提有誤) | ✅ **2026-08-18 完成**:已隨申請書〈陸〉送出並獲**原則核可**——scope `notification:push`、必帶 `X-User-Id`(須驗)+ `Idempotency-Key`、`source_app` 不取自 body、逐端點驗 scope | 1–2 h | 🟢 | — | ✅ 2026-08-18 |
| T14 | 推送 API:`POST /inbox/api/v1/notifications` 驗 S2S token + **逐端點驗 scope `notification:push`**(不只驗 `aud`)+ **`X-User-Id` 必驗**(UUID 形狀、不得等於呼叫方自身)+ **`Idempotency-Key` 去重** + `source_app` 由 client 身分推導;來源系統登記表;接入第一個來源(DI-4) | 無 token/錯 `aud`→**401**;**錯或缺 scope→403**(§11.5:401=憑證無效、403=有效但無權,**兩者必須分開**,否則呼叫方查不出是憑證錯還是授權不足);**未登記的 `azp`→403**(deny-by-default,**不因「來源是內網」放行——內網不是身分**);缺或壞的 `X-User-Id`→**400**;同 `Idempotency-Key` 重送→200 但不新增列(**窗口 24h,超窗視為新訊息**);body 帶假 `source_app` 被忽略(以 `azp` 查 DB 表);來源接入指南文件化(md+HTML) | 6–8 h(對方系統另計) | 🟢 | T13 ✅、**第一張憑證待 DI-4 立案**、DI-4 | ⬜ |

## M6 人對人站內信(階段二)

| 編號 | 任務 | 驗收標準(摘要) | 估時 | 影響 | 依賴 | 狀態 |
|---|---|---|---|---|---|---|
| T15 | DI-3 申請書:人對人顯示名稱之契約增修申請(提案:**另立一節**——寄件當下自寄件人本人 token 取 `name` 快照存入該則訊息;**不走 §4.2a 增訂級別**,因同儕可見是第三種失敗模式,契約 v3.1 已立下「失敗模式不同的東西不共用條文」的判準),援 upload-program 申請→裁決前例 | 申請書送達 cats-portal `DOCS/`;裁決結果回填本表與 CLAUDE.md A.2 | 1–2 h(+裁決) | 🟢 純文件 | **portal 承諾 M4 上線後與 rf-workorder #14 併案啟動** | ⬜ |
| T16 | 人對人:`direct` 類別+`thread_id` 對話串、寄信 UI、收件人選擇(名冊=已首登使用者,對齊 D7-7「首登才建列」)、`sender` 角色把關 | 無 `sender` 角色寄信 403;收件人只能選已首登者;對話串正確;顯示名稱**依 DI-3 裁決結果實作**,裁決前不顯示人名 | 10–14 h | 🟢 | T15 裁決、M5 | ⬜ |
| T17 | 階段二安全複驗+發版:XSS/白名單/SLO 迴歸、契約 §7 重跑、Release(憲法第八條:title+四段 content、版本常數先改) | 全測試綠;§7 全項通過或如實打叉;Release 頁四段齊 | 2–3 h | 🟢 | T16 | ⬜ |

---

## 計畫外交付(D 系列)

> `Tnn` 是 M0–M6 的施工任務;**`Dnn` 是 Benny 直接指示的文件交付**,不屬於任何里程碑。
> 分開編號是刻意的:混編會讓里程碑的完成度失真(文件做完了不代表功能做完了)。
> ⚠ 但仍必須列在這裡——**一件做過的事不在任務表上,下次盤點就會以為它沒做過**。

| 編號 | 任務 | 驗收標準(摘要) | 估時 | 影響 | 依賴 | 狀態 |
|---|---|---|---|---|---|---|
| D01 | 架構說明(商業邏輯 + 技術架構 + UI 概念圖):把散在上游規劃、開發計畫書與十幾篇 dev-log 的三件事收攏成一份給人看的文件;新增商業邏輯與 UI 概念圖兩張 SVG | md+HTML 並存且含 metadata/版本歷史;兩張新 SVG light 且**零外部依賴**;🔴 **新文件必須登記進 `render_docs.py` 的 TARGETS 與 `test_docs.sh` 的三份清單**,並**實際故意改壞一次**確認 `--check` 會紅(不登記的話守門會全綠——它根本沒去看那個檔);商業邏輯章節**必須寫出目前做不到的事** | 2–3 h | 🟢 純文件 | T08 | ✅ **2026-08-25**(`docs/架構說明.md`;文件層 40 → 45 項) |
| **D02** | 🔴 **憲法第九條指令位置標示改版**(Benny 2026-08-26 指示):`【機器】【路徑】【專案】` → `> 🖥️ 在哪執行 / 📄 編輯哪個檔 / 🏷️ 動到誰的東西` | 本 repo `CLAUDE.md` **v1.5**(11 款改寫 + **新增 13 款** + A.4 換實際值);`cats-portal/CLAUDE.md` **v1.20**(同一條規則在該檔出現**兩次**,兩處同批改);`docs/發版SOP.md` **v1.1**(五處指令改新格式,🔴 三處原本把檔名藏在指令裡的改為明列完整路徑);守門改驗新格式**且分三行各自報錯**,並實測「拿掉格式 → 紅」。**實測**:文件層 51 → **53 過 / 0 敗**,全 repo **233 項全綠** | 1 h | 🟢 純文件 | — | ✅ **2026-08-26** |
| **D03** | 🔴 **首次發版實測(v0.1.0)與「已知變成錯的話」回寫**:T10c 誠實標註「workflow 從未在真的 Actions 上跑過、映像不存在」,而 Benny 推 tag 後 **82 秒**那句話變假(第二條5)| `release-image #1` **success**(run `32963503452`,79 秒,8 步全過);映像 `ghcr.io/fttp0165/cats-inbox-api:0.1.0` 在 GHCR(digest `sha256:90d5a6f9…`);🔴 **兩件綠燈看不出來的事離開 GitHub 去驗**:`tags/list` 只有 `0.1.0`(**沒誤推 `latest`**)、匿名 manifest **HTTP 200**(→ **T11 不需 `docker login`**;GHCR 新套件預設 private,而 private 的症狀 `unauthorized` 會在低峰窗口出現);`發版SOP.md` **v1.2** §0 兩列 ⏳→✅、兩列維持 ⏳;🔴 **一條斷言的理由到期**(`assert "尚未" in text` 釘的是暫時狀態,四件事全完成才紅,而那天的正確反應是刪掉 §0 → 人會把「尚未」補回文件讓測試閉嘴)已改為釘 §0 表格結構,**兩種改壞方式都實測轉紅**。**實測**:全 repo **234 項全綠**(文件層 53 + pytest 181)| 1 h | 🟢 純文件 | T10c | ✅ **2026-08-26** |

---

## 版本歷史

| 版本 | 日期 | 修改人 | 摘要 |
|---|---|---|---|
| v1.20 | 2026-08-26 | Benny | **新增並完成 D03 → ✅**(首次發版實測與回寫)。🔴 **這一列記的是一句誠實標註「到期」的經過**:T10c 寫著「workflow 從未在真的 Actions 上跑過、映像目前不存在」,而 Benny 推 `v0.1.0` 之後 **82 秒**它就變假了 —— 第二條5 的正面情況(⚠ 錯的方向是**悲觀**,沒踩到「未跑過不得宣稱通過」的紅線,但 `發版SOP.md` 是**常設操作文件**,下一個照著做的人會看到 §0 說映像不存在而它就在那裡)。實測:`release-image #1` success(79 秒 / 8 步全過)、`ci #30` success。🔴 **兩件「綠燈看不出來」而特地離開 GitHub 去問 registry 的事**:①有沒有誤推 `latest`(workflow success 不會告訴你;實測 `{"tags":["0.1.0"]}`)②**映像匿名可拉**(HTTP 200)—— 後者對 **T11** 是硬需求:**GHCR 新套件預設 private**,而 private 的症狀是 VM 上 `docker compose pull` 回 `unauthorized`,**出現時機與 T10c 想擋掉的 `manifest unknown` 一模一樣**(低峰窗口、gateway 等著 reload),而「要先 `docker login`」當時不在任何一份部署文件裡。⚠ 另修一條**理由到期的斷言**:`assert "尚未" in text` 釘的是暫時狀態,四件事全部完成的那天才會紅,而那天的正確反應是刪掉 §0 —— 於是人會把「尚未」三個字補回文件讓測試閉嘴,**文件反而變成假的**;已改成釘 §0 表格的**結構**(每列須帶 ✅/⏳),兩種改壞方式都實測轉紅。同時把 T10c 列的誠實標註改為「寫於當時」+ 已執行 |
| v1.19 | 2026-08-26 | Benny | **新增並完成 D02 → ✅**(憲法第九條指令位置標示改版)。格式由 `【機器】【路徑】【專案】` 改為 `>` 引言區塊。🔴 **兩件比「換個排版」重要的事**:①**新增「📄 編輯哪個檔」**,補的是舊格式沒有的缺口 ——`$EDITOR app/__init__.py` 把檔名藏在指令裡,**相對於哪個目錄要讀的人自己推**,而推錯不會有錯誤訊息(改到另一個 repo 裡同名的檔,測試照樣綠因為改的那份沒進部署);`發版SOP.md` §1 原本就有三處這種寫法。②**新增第 13 款「🏷️ 動到誰的東西」**,承接舊格式的第三欄而非刪掉它 —— `docker exec portal-gateway nginx -s reload` 的**工作目錄根本不重要**,而它動的是綁 80/443、全 VM 唯一的 gateway;只有平台+工作目錄的話這個資訊沒有地方放。⚠ 生效範圍(沿 portal 第四條7 先例):自 2026-08-26 起對**新產出的內容**;**常設操作文件**(發版SOP)一併改寫,**dev-logs 與往來函件不動** ——竄改歷史紀錄比格式不一致更糟。portal 側 `CLAUDE.md` v1.20 同批改**兩處**(同一條規則在該檔出現兩次,只改一處會讓兩節互相矛盾)|
| v1.18 | 2026-08-25 | Benny | **新增並完成 T10c → ✅**(映像建置推送 + 發版 SOP)。🔴 **這一列補的是一個「所有文件都假設它已經存在」的東西**:compose 從 T01 起就引用 `ghcr.io/fttp0165/cats-inbox-api:<版本>`,而**沒有任何 workflow 建它、沒有任何任務提到它** —— 每份文件都對、每個任務也都完成了,而把它們接起來的那一步不在任何人的清單上;⚠ 症狀會在 **T11 的低峰窗口**當場出現,而那是最不該發現這件事的時刻。9 支測試綠(全 repo **231 項**),**15 項突變檢查全被抓到**。四項決策(只在推 tag 時建以免 Docker 建置吃光 Actions 額度 —— portal 正因額度用盡紅了一週;三道一致性檢查做成**會失敗的步驟**而不是靠人記得;**推之前先把映像跑起來驗**因為「建得起來」與「跑得起來」是兩件事;順手把 `test_docs.sh` 的三份手寫清單改成自 `TARGETS` 自動產生 —— 新增文件與納入守門變成同一個動作)。🔴 **誠實標註**:workflow 從未在真的 Actions 上跑過,映像**目前仍不存在**。⚠ 一項測試洞(SOP 四段檢查驗「字串在不在全文」而非「有沒有那個標題」)**與 T09b/T10b 同型:斷言的粒度比它宣稱保護的性質粗**,連續第三輪 |
| v1.17 | 2026-08-25 | Benny | **T10b 完成 → ✅,M3 全部結案**(CSRF 補齊 + 列舉式守門)。7 支測試綠(全 repo **216 項**),**13 項突變檢查全被抓到**。🔴 **三項發現,其中第一項是既有守門的洞**:①**T10 的「列舉所有 HTML 路由」守門實際上只驗到自動產生的文件頁** —— FastAPI 0.141 的 `_IncludedRouter` 讓 `app.routes` 只看得到文件路由,而該測試斷言「至少 3 個」剛好被 3 個文件頁滿足,**於是它一直全綠而四個真頁面一個都沒驗到**;與 D01 的發現同型(守門存在、看起來在運作、只覆蓋一部分對象),而 `_IncludedRouter` **T06 已經記過一次**——這是第二個受害者。已建 `iter_routes()` 並在守門補「必須列舉到 `/inbox/` 等三個真頁面」。②列舉式守門會打到 `/inbox/logout` 而它**清掉 session**,排在後面的頁面全變 401 而被靜靜跳過(改成每支之前重新登入,比維護「哪些路由有破壞性」的清單可靠)。③**我在加這道保護時把 401/403 的分界弄壞了**(沒有 session 時回 403),抓到它的是 **T09b 留下的 `test_unauthenticated_is_401`** —— 既有測試當安全網(第三條4)真的發揮作用的一次。⚠ 另有一個測試洞由突變抓到:只驗 `name="csrf_token"` 在不在,而把 token 改成空字串仍然綠(那會讓後台**完全不能用**),已改成同時驗值是 64 字元 HMAC |
| v1.16 | 2026-08-25 | Benny | **T09b 完成 → ✅**(公告發布頁)。17 支測試綠(全 repo **209 項**),**19 項突變檢查全被抓到**。五項決策:①**CSRF token 用 HMAC 綁 session**,不用簽章 —— 簽章會把 session key **明文帶到 HTML 上**,而那正是密封 cookie 在保護的東西;②**表單時間由頁面宣告時區**(`datetime-local` 不帶時區而 T09 的 API 拒收不帶時區的值)—— **宣告不是猜**,位移由 `FORM_TZ` 算出不寫死字面;③驗證+建立抽成一處讓 API 與表單共用(各寫一份會漂移,而**漂移之後寬的那一邊贏**);④成功回 **303** 而非 200(留在 POST 結果頁上按重新整理會**再發一則**);⑤入口連結只在真的有能力時顯示(顯示一個點下去會 403 的連結比不顯示更糟)。🔴 **三個測試洞全部由突變檢查找出**,共同形狀是**斷言的對象裡本來就含有我要找的字串**:拿 `cookie.split(".")[0]` 當 session key(那是 base64 不是 key)、測試資料用「標題」「內容」而那兩個詞在 `<label>` 裡本來就有、對整頁 `re.search` 欄位名而非只在錯誤框內比對 —— 三者都讓測試看起來嚴格而**永遠通過**。這是突變檢查第一次抓到**測試本身**的邏輯錯誤(前幾輪都是量測工具問題)。⚠ **一項新發現的缺口**:角色後台的「清除顯示名稱」也是 POST 表單而**它沒有 CSRF**,已列為 T10b |
| v1.15 | 2026-08-25 | Benny | **T10 完成 → ✅,M3 結案**(安全紅線落地)。41 支測試綠(全 repo **192 項**),**19 項突變檢查全被抓到**。反例由原訂 4 則擴為 **14 則**,新增的三則是「看起來會過」的經典洞:**`/\` 也是協定相對**(瀏覽器正規化成 `//`)、**前綴冒充** `...com.tw.evil.tld`(通過 `startswith` 而網域是別人的)、**userinfo 冒充** `...com.tw@evil.tld`。🔴 **一項發現**:**嚴格 CSP 連 `style="…"` 屬性也擋**,而 nonce 只對**元素**有效 —— 「`<style>` 帶了 nonce」**不等於**「樣式會生效」;原有 5 個行內樣式屬性全部改 class,不改的症狀是**版面走鐘而伺服器零錯誤**(與 portal 2026-08-03 那次同型)。🔴 **量測工具第三次出問題,而這次會產生假 ✅**:突變腳本把 pytest 的非零離開碼一律讀成「抓到」,而 `-k` 打錯時 **rc=5(全 deselect)/ rc=4(無此測試)也是非零** —— **選擇器打錯會讓每個突變都看起來被抓到**;前兩次(T06 sed 沒配到、T09 sed 改錯語意)偏向假 ❌ 而這次偏向假 ✅。已修腳本並重測,19 項是修好後的數字。順帶給 **T12** 補一條(iframe 嵌入前必須先改 `frame-ancestors`)、給 **T09b** 補一條(第一個 POST 表單,CSRF 須同批做完) |
| v1.14 | 2026-08-25 | Benny | **T09 完成 → ✅**(公告:發布 / 有效期 / active / 逐人已讀)。21 支測試綠(全 repo **151 項**),**17 項突變檢查全被抓到**。🔴 **範圍擴充(第一條4)**:原 T09 只有三個 API,而 M4 叫「公告中心上線」、其下 T11/T12 都不含公告的畫面——**照原表走完 M4,公告上線後沒有任何畫面顯示它**,而每一層都回 200。故把「收件匣頁顯示公告」併入 T09,並**另立 T09b** 收發布表單(不混進來:它需要表單/CSRF/錯誤 UI,估時與 T09 相當)。同時給 **T12 補一條驗收**:鈴鐺要不要數公告必須明講,否則公告在入口頁零信號。三個刻意決定:①**不帶時區的時間一律 400**(台灣的 `02:00` 當 UTC 存會差 8 小時而零錯誤,portal 憲法第四條7 的同一種坑),②公告不存在回 **404** 而訊息回 403(差別在**有沒有歸屬要保護**),③`active` **不另發 count 查詢**(兩次查詢會對不起來,而那個畫面看起來像資料庫壞了)。🔴 順手修掉跨 T08 的一致性問題:時間欄位在 SQLite 上不帶時區、PG 上帶——**本機測試與正式環境行為不同**,已統一走 `iso_utc()`。⚠ 第一輪突變有 1 項顯示未抓到,查為 **sed 沒真的改到語意**(加了 `True,` 而條件還在),改對後立刻紅——**工具錯,不是測試有洞**;與 T06 的「拿掉 no-store」是同一種誤判 |
| v1.13 | 2026-08-25 | Benny | **新增「計畫外交付(D 系列)」一節 + D01 完成 → ✅**(Benny 2026-08-25 指示的架構說明)。分開編號是刻意的:`Dnn` 混進 `Tnn` 會讓里程碑完成度失真(文件做完不代表功能做完);但仍必須列在表上——**一件做過的事不在任務表上,下次盤點就會以為它沒做過**。D01 交付 `docs/架構說明.md`+`.html` 與兩張新 SVG(商業邏輯、UI 概念圖),沿用既有的架構總覽。🔴 驗收明訂**新文件必須登記進 `render_docs.py` 的 TARGETS 與 `test_docs.sh` 的三份清單**,且要**實際故意改壞一次**確認 `--check` 轉紅——不登記的話守門會全綠,因為它根本沒去看那個檔(與 2026-08-24 在 portal 修的「函件守門只看單向」同型)。已實測:改壞→紅、還原→綠;文件層 40 → **45 項** |
| v1.12 | 2026-08-25 | Benny | **T08 完成 → ✅**(讀取 API + 收件匣 UI)。14 支測試綠(全 repo 125 項),**9 項突變檢查全被抓到**。🔴 **Bootstrap 沿用平台既有那一份**:本環境無法對外取得(proxy 403),而 `portal-admin` 已本地託管 5.3.8/MIT——複製過來且**雜湊與其逐字相同**(`8f8173cb…e4ff`),比「從網路抓一份」更可驗證;照 portal 慣例留 LICENSE/PROVENANCE/sha256,並**繼承其 2026-08-03 踩過的坑**(檔案不在 `StaticFiles` 掛載目錄下時 app 不送出去,症狀是「整頁沒樣式而伺服器零錯誤」),故加一支**打實際 URL** 的守門測試。🔴 突變檢查補到一個測試的洞:零人名原本只驗 HTML 頁面,而往 API 回應加一個姓名欄位**測試照樣綠**(模板沒算繪它,但前端 JS 拿得到,而那正是 §4.2a L1 要禁的)——已補「API 回應不得含姓名類欄位」的結構性斷言。⚠ 另記一件**量測工具**的問題:突變腳本的 write→test→restore 在同一秒內完成時,還原後檔案的 (mtime, size) 可能與 `.pyc` 相符,Python 沿用由**突變後原始碼**編出的 bytecode,使突變**留在工作目錄裡**(症狀是「原始碼看起來對而行為是錯的」);已在腳本加 `__pycache__` 清除 |
| v1.11 | 2026-08-24 | Benny | **T07 完成 → ✅**(三張表 + migration `0002`)。10 支測試綠(全 repo 111 項),**9 項突變檢查全被抓到**。🔴 **補了 T05 誠實標註的盲點**:`test_models_match_migration_schema` 在同一個 PostgreSQL 上開兩個資料庫,一邊跑 migration、一邊跑 `create_all()`,逐表逐欄比對——在此之前**沒有任何一支測試在比較兩者**,而漂移的症狀是「本機全綠、部署後少一個欄位」。三個刻意決定:①`message.recipient_sub` **不設外鍵**(推送 API 必須能推給從未登入過的人,設了外鍵會讓通知被資料庫**靜默**丟掉),②`announcement_read.user_sub` **要設**外鍵 CASCADE(與①相反是刻意的),③`category`/`audience` 用 String+CHECK 而非 PG enum(enum 加值要 `ALTER TYPE`,寫不出 downgrade)。🔴 **主動揭露**:上游 §4.2 的 `audience` 支援「群組」,而群組定向需 `groups` claim、我方 client 刻意未申請——欄位照建、CHECK 擋在 `all`,要開放須先向 portal 申請 §4.2a **L2**。⚠ 欄位名照上游 §4 **逐字沿用**(改名會讓上游 §2.1「退路=搬四張表」變成一次資料遷移)|
| v1.10 | 2026-08-24 | Benny | **T05 完成 → ✅**(首登建號 + 自動授 reader + 角色後台)。13 支測試綠(全 repo 101 項),**10 項突變檢查全被抓到**。🔴 **排程變更(第一條4)**:`app_user` 與 Alembic 基礎設施**由 T07 提前到 T05**——T05 有四條驗收是**資料庫斷言**(「DB 只多一列」「reader 可單獨停用」「快取清除工具實測」),而記憶體版讓這些話**失去意義**:清除工具會變成永遠成功的空操作(**比沒有工具更糟**),C2/C4 也給不出「重啟後仍然是關的」狀態。T07 因此縮為三張表、估時 3–4h → 2–3h、依賴改為 T05。DEC-16 四條件逐條落地:C1=`ROLE_CAPABILITIES` 模組層常數、C2=`user_role.enabled`(per-user)+ `INBOX_AUTO_GRANT_READER`(全域)、C3=兩支反向測試、C4=全域開關關掉即全 deny。⚠ **本機只有 PG16.13**,**PG15 演練留 T11**——不得視為已符合 PG15 紅線 |
| v1.9 | 2026-08-24 | Benny | **T06 完成 → ✅**(登出雙向)。9 支測試綠(全 repo 85 項)。🔴 **兩處來源文件同一次提交回寫**(第二條5):①`docs/契約對齊盤點_v3.3.md` v1.1——§4 Q1 覆 portal 的答案由 `/inbox/` 改為 **`/inbox/logged-out/`**(原答案的 `/inbox/` **T08 才存在**、且登出後立刻被導去登入頁會讓使用者看不出登出成功沒有;新值已在 client 登記值內,**portal 零改動**,且**覆函尚未發出故零成本**);②`docs/TDD測試計畫表.md` v1.4——T06 規格由 3 支擴為 7 支,並更正「帶斜線變體必須 404」(實測 FastAPI 回 307 且保留 `sid`/`iss`,那是**安全網**不是危害)。⚠ 突變檢查抓到一個真的洞:原測試只斷言登出後 `/me` 回 401,而**刪 cookie 也會讓它 401**,伺服器端 session 其實還活著 |
| v1.8 | 2026-08-21 | Benny | **T04 完成 → ✅**(OIDC 登入)。12 支 auth 測試綠(全 repo 76 項);🔴 **找到一處會在 T11 才爆的不一致**:`.env.example` 的 `INBOX_OIDC_INTERNAL_BASE` 帶了 `/realms/sporton`,而 portal 交付檔給的是 `http://keycloak:8080/auth`(不含 realm,realm 由程式自 ISSUER 推導後接上)——照舊值部署會組出 `/realms/sporton/realms/sporton`,discovery 404,而**本機離線測試全綠**。已修並加測試釘住形狀。另補三項規格外的測試(state 不符 → 400、refresh 失敗 → 401 登出、設定不齊備時不註冊 auth 路由=回滾閥門),TDD 計畫表同步升 v1.3 |
| v1.7 | 2026-08-18 | Benny | **T03b 完成 → ✅**(逐條對齊 14 版,產出 `docs/契約對齊盤點_v3.3.md`)。三處驗收因此擴充:**T04** 加「印出實際 `redirect_uri` 逐字比對」(契約 v2.14 前例:PLM 首登即 mismatch 而 app 的 log 是空的)與「用壞 code 打 token 端點驗 secret」(v2.17,不需真人登入);**T06** 加「route 字串與 client 登記值逐字相同、無結尾斜線」(§10.3a,差一字元=端點零呼叫);**T14** 依 §11.5 改為 **401 與 403 分離** + 未登記 `azp` 一律 403(**內網不是身分**)+ 去重窗口 24h |
| v1.6 | 2026-08-18 | Benny | **client 已核發回寫**:T03 驗收欄由「client 仍未建立」改為已建立(`a51d9925-…`,回讀四組全過、`email` scope 已移除);⏳ 但 **secret 尚未進本服務 `.env`**——`/opt/cats-inbox` 尚不存在,T11 部署時併入(portal EXT-13)。T04 註記改為「本機開發可用真 issuer + 假 token,正式端到端要等部署後拿到 secret」 |
| v1.5 | 2026-08-18 | Benny | **新增 T03b:逐條對齊契約 v3.3,列為 T04 前置**。理由:T03 送件時才發現我方 A.2 記著 v2.7 而實際已是 v3.3(差 §11 整章),而**沒有任何機制會在送件前提醒去看一眼**;不先對齊就動工 T04,驗收基準可能是舊契約 |
| v1.4 | 2026-08-18 | Benny | **portal 回覆回寫**:T03 → ✅(獲覆,但 client 仍未建立=portal EXT-11);**T13 → ✅** 並更正其前提(原寫「S2S 章節送件」,章節早於契約 v3.0 定稿,實際送出的是 §11.7 寫入型申請);T05 驗收加入 DI-2 核可後的 `reader` 自動授與其四條件(反向測試、可單獨停用);T14 驗收具體化為五項可測行為(錯 scope→401、壞 `X-User-Id`→400、同 key 重送不新增列、假 `source_app` 被忽略) |
| v1.3 | 2026-08-17 | Benny | **T02 完成 → ✅**:portal《站內信通知中心設計規劃》v1.4 選址回寫(連帶修三處已知錯誤、N4 結案、新增 N6);portal `CLAUDE.md` v1.12 系統清單登記;其待辦清單 v2.2 掛入四項待決(建 client / 開通偏離裁示 / 姓名議題併案 / S2S 章節)。已推 portal 分支未開 PR。本檔 A.2 對齊版本同步更新(憲法 v1.2) |
| v1.2 | 2026-08-17 | Benny | **T03 送件完成 → 🔵**(不標 ✅:client 尚未核發,標 ✅ 等於謊報)。申請書含 §5 五項必填 + session 形態聲明 + DI-2 偏離申請;副本已送 cats-portal。驗收欄改列「待 portal」四項具體回覆物 |
| v1.1 | 2026-08-15 | Benny | **T01 完成**(M1 骨架):驗收欄改為實測結果(23 項 pytest 全綠、compose 解析通過、健康檢查不查 DB 已以壞 DB 位址證明),並誠實標註「容器實際啟動未驗」留 T11 於 VM 補;導言加入 `docs/TDD測試計畫表.md` 的對應關係(本表管做什麼、該表管怎麼證明) |
| v1.0 | 2026-08-15 | Benny | 初版:依開發計畫書 v1.0 §4 展開 M0–M6 共 18 個任務(T00–T17),各附驗收標準摘要、估時、影響分級、依賴;T00 標記完成 |
