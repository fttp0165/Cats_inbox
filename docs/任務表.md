# cats-inbox 任務表

**建立日期:** 2026-08-15 08:55
**最後更新:** 2026-08-15 10:55
**版本:** v1.2

> 依 `docs/開發計畫書.md` §4 展開;狀態 ⬜ 未動工 / 🔵 進行中 / ✅ 完成(憲法第一條2)。
> **每個任務的紅測試規格見 `docs/TDD測試計畫表.md`**(同一組 Tnn 編號:本表管「做什麼」,該表管「怎麼證明做到了」)。
> 影響分級(對現有服務/資料):🟢 無影響 / 🟡 需低峰或協調 / 🔴 不可做。
> 每任務動工前依第二條在 `docs/dev-logs/` 建日誌(目標/影響範圍/驗收標準/回滾方式),完工補結果。

---

## M0 採用與計畫

| 編號 | 任務 | 驗收標準(摘要) | 估時 | 影響 | 依賴 | 狀態 |
|---|---|---|---|---|---|---|
| T00 | 憲法 + 開發計畫書 + 任務表 + 文件工具 + CI | `tests/test_docs.sh` 全綠;md+HTML 並存且 `--check` 同步;憲法附錄 A 特化完成 | 4 h | 🟢 純文件 | — | ✅ 2026-08-15 |

## M1 骨架

| 編號 | 任務 | 驗收標準(摘要) | 估時 | 影響 | 依賴 | 狀態 |
|---|---|---|---|---|---|---|
| T01 | 服務骨架:FastAPI `/inbox/health`(不查 DB)、Dockerfile(non-root、二段建置)、compose(`cats-edge` external 治本寫法 + `cats-inbox-pg` 僅內網)、`.env.example`、`test_skeleton.py`+`test_app.py` 紅→綠、CI 跑 run_all | ✅ 23 項 pytest 全綠(骨架 19 + 應用 4);`docker compose config` 解析通過;DB 指向壞主機時 `/inbox/health` 仍 200(證明不查 DB);pg 無 ports、不在 cats-edge;`.env.example` 涵蓋 config.py 全部變數。⚠ **容器實際啟動未驗**(本環境無 docker daemon),留 T11 於 VM 補 | 5–7 h | 🟢 | — | ✅ 2026-08-15 |
| T02 | 回寫 portal《站內信通知中心設計規劃》:選址由「掛 compliance」改為獨立服務 cats-inbox,升版並留版本歷史(DI-1) | cats-portal 側 PR 合併;該文件 §2 與版本歷史更新;md+HTML 同步(依 portal 憲法) | 1–2 h | 🟢 純文件(portal repo) | DI-1(已定) | ⬜ |

## M2 SSO 接入

| 編號 | 任務 | 驗收標準(摘要) | 估時 | 影響 | 依賴 | 狀態 |
|---|---|---|---|---|---|---|
| T03 | 依契約 §5 申請 client:`cats-inbox`、confidential+PKCE、redirect URI `https://catsapp.sporton.com.tw/inbox/oidc/callback/`、**session 形態聲明=cookie/伺服器 session**、需要 claims:`name`;隨函附 DI-2 開通政策偏離申請 | ✅ **申請書已送出**(`docs/SSO接入申請.md` v1.0;副本 `cats-portal DOCS/cats-inbox_SSO接入申請_20260815.md`,已推其分支)。⏳ **待 portal**:受理、建立 client、交付 `.env.keycloak` 樣板、DI-2 裁示、§9 登記表掛號。secret 走安全管道不進 git/聊天 | 1 h(+portal 作業) | 🟢 | T01 | 🔵 送件完成待回覆 |
| T04 | OIDC 登入:Authorization Code+PKCE、state/nonce、RS256/JWKS(1h 快取、kid 輪替)、驗 `iss`/`aud`/`exp`±30s、容器內走 `OIDC_INTERNAL_BASE` 而 `iss` 不改、**伺服器端 refresh_token 主動續期**(§3.3 陷阱) | auth 測試綠:竄改簽章/過期/錯 aud→401;登入後過 300s 頁面仍有效(續期證據);測試替身含過期案例 | 6–8 h | 🟢 | T03 | ⬜ |
| T05 | 首登建號+待開通+角色後台:user 表只存 `sub`+角色;待開通頁顯示本人 `sub`;bootstrap 管理員清單**每次登入比對**(idempotent、disabled 不復活);admin 派角色 UI;`display_name` L1 快取(僅本人 token 寫入、僅管理後台顯示、附整批+單筆清除工具) | 首登 DB 只多一列(無 email/密碼欄);無角色打業務 API 一律 403;清單內帳號再登入自動升級;快取清除工具實測 | 5–7 h | 🟢 | T04 | ⬜ |
| T06 | 登出雙向:自發登出(先清自己 session → 導 IdP end_session);SLO front-channel 端點 `GET /inbox/oidc/frontchannel-logout`(免認證、冪等、只刪 cookie、204、`iss`/`sid` 可有可無);端點上線後知會 portal 註冊 | 冒煙:入口登出→換帳號→進 inbox 不得仍是前一人(**測前清 cookie/無痕**,§10.7);重複呼叫端點皆 204 | 2–3 h | 🟢 | T04 | ⬜ |

## M3 通知核心(公告先行)

| 編號 | 任務 | 驗收標準(摘要) | 估時 | 影響 | 依賴 | 狀態 |
|---|---|---|---|---|---|---|
| T07 | 資料模型與 migration:`app_user`/`message`/`announcement`/`announcement_read`(Alembic,每條附 backward) | 本機 up→down→up 演練通過;schema 無 email/姓名/密碼欄(`display_name` 除外且 nullable) | 3–4 h | 🟢(新庫) | T01 | ⬜ |
| T08 | 讀取 API + 收件匣 UI:列表(unread 過濾)、`unread-count`、標已讀(冪等)、收件匣頁(base path `/inbox/`、Bootstrap 5 本地託管、light) | deny-by-default 測試:讀他人訊息 403;`sender_sub` 不信前端;UI 零外部 CDN;階段一 UI **零人名**(source_app 標籤) | 6–8 h | 🟢 | T05、T07 | ⬜ |
| T09 | 公告:發布(`announcer` 角色)、有效期 starts/ends、`announcements/active`、公告已讀(AnnouncementRead) | 非 announcer 發布 403;過期公告不出現在 active;已讀逐人記錄不複製列 | 3–4 h | 🟢 | T08 | ⬜ |
| T10 | 安全紅線落地:body 純文字+輸出跳脫、`action_url` 同站白名單(寫入時 400 拒)、內容不進 log、嚴格 CSP+nonce | 紅測試先行且含反例:`<script>`、外部網址、`javascript:`、`//` 協定相對、超長輸入;log 抽查無 subject/body | 2–3 h | 🟢 | T08 | ⬜ |

## M4 掛上入口(公告中心上線)

| 編號 | 任務 | 驗收標準(摘要) | 估時 | 影響 | 依賴 | 狀態 |
|---|---|---|---|---|---|---|
| T11 | gateway 路由:cats-portal 提 PR(`/inbox/` → `cats-inbox-api:8080` + proxy_headers);VM 部署;低峰套用 | 容器在 `cats-edge` 短名可解析;`nginx -t` 過;`curl -skI https://catsapp.sporton.com.tw/inbox/health` 200;既有路由(`/plm/` `/TMP_GEN/` 等)zero-diff;可秒級回滾 | 2–3 h(+portal 側 1–2 h) | 🟡 gateway reload | T01–T10 | ⬜ |
| T12 | 上線冒煙 + landing 鈴鐺:跑契約 §7 全清單(未完成項如實打叉);portal-landing 加鈴鐺(30s 輪詢 `unread-count`,portal 側施工) | §7 清單結果寫入 dev-log;鈴鐺在 IdP/inbox 不可用時不擋首頁(漸進增強);上線公告發出 | 3–4 h(+portal 側 2–3 h) | 🟡 landing 改版 | T11 | ⬜ |

## M5 系統通知開通

| 編號 | 任務 | 驗收標準(摘要) | 估時 | 影響 | 依賴 | 狀態 |
|---|---|---|---|---|---|---|
| T13 | S2S 需求送件:向 portal 提出被呼叫方驗證規格需求(audience/scope 切法、來源系統辨識),與 rf-workorder/報告產生系統兩案一併規格化 | 需求文件送達 cats-portal `DOCS/`;S2S 章節草案含 inbox 場景 | 1–2 h(+portal 定稿) | 🟢 | M4 | ⬜ |
| T14 | 推送 API:`POST /inbox/api/v1/notifications` 驗 S2S token(client_credentials);來源系統登記表;接入第一個來源(DI-4) | 無 token/錯 audience→401 測試綠;來源系統以自己的 S2S client 推送成功、收件人鈴鐺亮;來源接入指南文件化(md+HTML) | 6–8 h(對方系統另計) | 🟢 | T13、S2S 章節定稿、DI-4 | ⬜ |

## M6 人對人站內信(階段二)

| 編號 | 任務 | 驗收標準(摘要) | 估時 | 影響 | 依賴 | 狀態 |
|---|---|---|---|---|---|---|
| T15 | DI-3 申請書:人對人顯示名稱之契約增修申請(提案:寄件當下自寄件人本人 token 取 `name` 快照存入該則訊息;或 §4.2a 增訂級別),援 upload-program 申請→裁決前例 | 申請書送達 cats-portal `DOCS/`;裁決結果回填本表與 CLAUDE.md A.2 | 1–2 h(+裁決) | 🟢 純文件 | 建議 M4 後即送 | ⬜ |
| T16 | 人對人:`direct` 類別+`thread_id` 對話串、寄信 UI、收件人選擇(名冊=已首登使用者,對齊 D7-7「首登才建列」)、`sender` 角色把關 | 無 `sender` 角色寄信 403;收件人只能選已首登者;對話串正確;顯示名稱**依 DI-3 裁決結果實作**,裁決前不顯示人名 | 10–14 h | 🟢 | T15 裁決、M5 | ⬜ |
| T17 | 階段二安全複驗+發版:XSS/白名單/SLO 迴歸、契約 §7 重跑、Release(憲法第八條:title+四段 content、版本常數先改) | 全測試綠;§7 全項通過或如實打叉;Release 頁四段齊 | 2–3 h | 🟢 | T16 | ⬜ |

---

## 版本歷史

| 版本 | 日期 | 修改人 | 摘要 |
|---|---|---|---|
| v1.0 | 2026-08-15 | Claude(Benny) | 初版:依開發計畫書 v1.0 §4 展開 M0–M6 共 18 個任務(T00–T17),各附驗收標準摘要、估時、影響分級、依賴;T00 標記完成 |
| v1.2 | 2026-08-15 | Claude(Benny) | **T03 送件完成 → 🔵**(不標 ✅:client 尚未核發,標 ✅ 等於謊報)。申請書含 §5 五項必填 + session 形態聲明 + DI-2 偏離申請;副本已送 cats-portal。驗收欄改列「待 portal」四項具體回覆物 |
| v1.1 | 2026-08-15 | Claude(Benny) | **T01 完成**(M1 骨架):驗收欄改為實測結果(23 項 pytest 全綠、compose 解析通過、健康檢查不查 DB 已以壞 DB 位址證明),並誠實標註「容器實際啟動未驗」留 T11 於 VM 補;導言加入 `docs/TDD測試計畫表.md` 的對應關係(本表管做什麼、該表管怎麼證明) |
