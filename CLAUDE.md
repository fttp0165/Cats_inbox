# cats-inbox 開發憲法(CLAUDE.md)

**建立日期:** 2026-08-15 08:45
**最後更新:** 2026-08-15 10:15
**版本:** v1.1

> 本檔為本專案的最高開發準則,所有開發行為(含 AI 協作)必須遵守。
> **卷一**管「怎麼把東西做出來」,**卷二**管「怎麼把過程講清楚」。兩卷同時生效,不因話題改變而失效。
> 本專案內與其他文件衝突時以本檔為準;修改本檔需在文末「版本歷史」留下紀錄。
> 例外見〈位階〉:上位技術規約的偏離權不在本專案。
> **專案特化內容一律填在〈附錄 A〉**,不散落在條文裡——條文可跨專案原封複製。

---

## 位階

```
上位技術規約 ── 帳號系統接入契約(SSO)/ App服務對齊指南(gateway)/ portal 平台紅線
        │        管「做出來的東西要長怎樣」;偏離鐵則需 portal/Benny 同意,本專案無權自行豁免
        ▼
本開發憲法(卷一 流程八條 + 卷二 輸出三條)
        │        管「怎麼把東西做出來」與「怎麼回報」;本專案內最高
        ▼
docs/開發計畫書.md → docs/任務表.md → docs/dev-logs/
```

上位規約與本檔管轄不重疊。本檔「以本檔為準」的效力**限於開發流程與回應格式**;上位鐵則不因本檔而鬆動。
上位文件一律**只引用、不複製維護**(避免兩個真相),並在〈附錄 A.2〉記錄已對齊的版本與日期。

---

## 衝突裁示(兩卷交界處)

| 情境 | 裁示 |
|---|---|
| 文件要帶 metadata(第七條),但回應禁止前言(第九條) | 第九條只管**對話回應**;寫進 `docs/` 的文件照第七條帶齊日期、版本、歷史 |
| 動工前要有書面計畫(第二條),但回應要先給下一步(第九條) | 計畫寫進 `docs/`;回應第一行是「現在跑哪一步」,附計畫檔路徑,不重述計畫全文 |
| 清單上限 5 項(第九條),但驗收標準有 9 條 | 條列本身就是答案時,任務贏(第十條);拆成「現在做 / 稍後做」 |
| 回應要給下一步,但那步是 AI 自己做得到的 | AI 自己做完再報已驗證的結果(第十條);「下一步」不得用來把自己的工作丟回人類 |
| 上位規約與本檔衝突 | 上位規約贏,並在〈附錄 A.2〉記錄對齊版本 |

---

# 卷一:開發流程

## 第一條:敏捷式開發

1. 以**小步快跑**為原則:每個任務是一個獨立可交付、可回滾的增量,不做大爆炸式改動。
2. 依任務表的里程碑順序推進;每完成一個任務即更新任務表狀態(⬜→🔵→✅)。
3. 每個增量完成的定義(Definition of Done):
   - 驗收標準達成
   - 測試全綠(CI 上線後含 CI 綠燈)
   - 開發日誌已寫(第六條)
   - 相關文件已更新版本與歷史(第七條)
4. 需求變更不直接改程式:先更新開發計畫書/任務表,再依新任務執行。

## 第二條:先寫開發計畫

1. **任何開發動作之前,必須先有書面計畫**。無計畫不動工。
2. 小任務:**動工前**先在 `docs/dev-logs/` 建立該任務的日誌檔,寫妥「目標、影響範圍、驗收標準、回滾方式」
   四項計畫段落,完工後在同一檔補上結果——不得事後補寫計畫段。
   (若確實發生事後補寫,必須在日誌開頭明確標示「**本篇為事後補記,違反第二條**」,不得偽裝流程跑過。)
3. 大任務(新功能、資料遷移、重構):先在 `docs/plans/` 建立獨立計畫文件,經確認後才動工。
4. 計畫必須明確標註**對現有資料的影響**(不動 / 加欄位 / UPDATE / 刪除),分級 🟢🟡🔴。
5. 執行中發現計畫與現實不符:除了記錄於日誌與任務表,**必須回寫修正到來源計畫文件**並升版,
   不得讓計畫書留著已知錯誤的內容。

## 第三條:依需求先寫測試(TDD)

1. **先寫一個「現在會失敗」的紅測試**,把預期行為釘住,才開始修改程式;修正完成的定義就是測試轉綠。
2. 測試永久保留在 `tests/`,納入 CI 防止回歸。
3. 測試資料一律在 fixture 現造假資料;**嚴禁**將任何真實個資、正式資料匯出檔放進測試與 CI。
4. 純重構可不寫新測試,但**既有測試必須維持全綠**作為安全網。
5. 「測試全綠」指**實際跑過並看到綠燈**;未執行的測試不得宣稱通過。

## 第四條:文件一律 Light 版本,md 與 HTML 並存

1. 所有產出的文件(HTML 報告、圖表、簡報、匯出頁面)**一律使用淺色(light)主題**,白底深字。
2. 禁止深色背景配色;不使用 `prefers-color-scheme: dark` 自動切換為深色。
3. 圖表配色以淺色背景可讀為準。
4. **正式文件必須同時提供 Markdown 與 HTML 兩種版本**(開發計畫書、任務表、架構/設計文件、SOP、報告):
   md 為權威來源(進版控、供審閱),HTML 為單檔可下載的發布版;兩版內容必須同步更新,歧異時以 md 為準。
   開發日誌等內部紀錄可僅提供 md。
5. **HTML 版含架構圖/流程圖時,一律以內嵌 SVG 呈現**(light 配色、無外部依賴、單檔離線可開);
   md 版以對應的文字圖(ASCII)呈現即可。
6. HTML 版由 `python tools/render_docs.py` 自 md 產生,避免兩版人工同步而漂移;
   圖示以 `<!--SVG:名稱-->` 標記,產生時抽換為 `docs/assets/名稱.svg`。

## 第五條:必須寫註釋

1. 所有新增或修改的程式碼必須附上**中文註釋**,說明「為什麼這樣做」(意圖與限制),而不只是「做了什麼」。
2. 函式/類別必須有 docstring:用途、參數、回傳值、副作用(特別是會動到資料庫或物件儲存的)。
3. 複雜邏輯(串流處理、格式判定、權限判定、資料遷移)必須逐段註釋。
4. 修 bug 時在修正處註明根本原因,避免後人改回去。

## 第六條:寫完後必須寫開發日誌

1. 每完成一個任務,在 `docs/dev-logs/` 新增一篇日誌,檔名格式:`YYYY-MM-DD_<任務編號>_簡述.md`
   (例:`2026-07-25_T01_專案骨架.md`)。
2. 日誌必要內容:
   - **日期時間**與**對應任務編號**
   - 做了什麼(異動檔案清單)
   - 為什麼這樣做(決策理由、捨棄的替代方案)
   - 測試結果(紅→綠的證據)
   - 對現有資料的實際影響
   - 遺留問題與後續建議
3. 當天未完成的工作也要寫進度日誌,標明中斷點,方便接續。

## 第七條:文件必須標註日期時間、版本與歷史

1. 所有文件(計畫書、任務表、開發日誌、README、設計文件)開頭必須有:

   ```markdown
   **建立日期:** YYYY-MM-DD HH:MM
   **最後更新:** YYYY-MM-DD HH:MM
   **版本:** vX.Y
   ```

2. 文件末尾必須維護「版本歷史」表:

   ```markdown
   ## 版本歷史
   | 版本 | 日期 | 修改人 | 摘要 |
   |---|---|---|---|
   | v1.0 | 2026-07-25 | <姓名> | 初版 |
   ```

3. 版本規則:內容實質變更升次版號(v1.1→v1.2),結構性/方向性大改升主版號(v1.x→v2.0)。
4. 每次修改文件,**必須同時更新「最後更新」時間與版本歷史**,缺一不可。

## 第八條:發版儀式(Release 必附 title 與 content)

1. **每次發布 Release 必須提供 title 與 content**,不得只推一個裸 tag。
   Release 頁是使用者與未來的自己唯一會看的「這一版是什麼」——tag 本身說不出任何事。
2. **title 格式:** `vX.Y.Z — 一句話重點`。
3. **content 必含四段**(缺一不可):
   - **本版內容**:任務編號 + 一句話
   - **對現有資料的影響**:🟢 不動 / 🟡 加欄位 / 🔴 UPDATE 或刪除;**有無 migration**
   - **部署注意**:`.env` 是否新增變數、是否需跑 migration、換版後的驗證點
   - **測試**:該版的測試總數與 CI 綠燈狀態
4. 🔴 **版本號常數必須在打 tag 之前隨 PR 改成該版**。
   版本號會顯示給使用者,tag 與常數不一致等於說謊,換版驗證也會判錯。
5. **同一個版本 tag 不重發**;發錯就發下一個 patch 版。
6. 發版前一分鐘檢查:驗 migration 鏈完整、有 migration 的版本本機 up→down→up 演練過。

---

# 卷二:協作與輸出

> 本卷管的是**對話回應**(含 AI 協作 session 的每一次回覆),不是 `docs/` 裡的正式文件。
> 讀者有 ADHD。這些規則適用於每一次回應,不只第一次,不因話題改變而失效。
> 若不確定是否還適用——還適用。

## 第九條:回應格式

**結構**

1. **開頭就是下一個動作**:指令、路徑或程式片段擺第一行。不是背景,不是計畫。
2. **多步驟工作編號**:一步一個有界動作。用能跑得動的最少步數——走完短路徑勝過放棄完整路徑。
3. **結尾給一個具體動作**,兩分鐘內做得到。「打開這個檔」就算。
4. **清單上限 5 項**:超過就拆成「現在做 / 稍後做」。五項排序過勝過十項沒排序。

**狀態(不要求對方記著)**

5. **每一輪重述進度**:「5 步做完 3:schema 已更新。下一步:回填欄位。」
   不要求人類在訊息之間保存狀態。有任務/計畫工具就用它,讓清單去重述。
6. **先解決當前問題再提新的**:若存在第二個問題,只在結尾提一次,當成獨立問句。
7. **改完之後直接展示現在能做什麼**,以及怎麼親眼看到。不要把成果埋在總結裡。

**精確**

8. **時間估計用具體單位**:「測試已覆蓋的話約 15 分鐘」——不准說「有點工作量」。
   時間指向**實際執行那步的人**。
9. **錯誤:位置、原因、修法**。不要「糟糕」,不要「似乎有點問題」。
   好例:「`auth.spec.ts:42` 失敗:預期 200,得到 401。原因:缺 auth header。
   修法:加 `Authorization: Bearer ${token}`。」

**禁止**

10. **無前言、無總結、無收尾語**。
    禁用:「好問題」「讓我來…」「當然!」「看了你的…」「希望有幫助」「還有其他需要幫忙的嗎?」

**指令必須標明「在哪裡下」**

> 🔴 本節**附在第九條最後**,不插進 1–10 之間——插隊會讓既有引用悄悄指到別的東西。
> **新增條文一律往後加。**

11. 給任何指令(shell、docker、git、SQL)之前,**先標明三件事**,缺一不可:

    | 要標的 | 說明 |
    |---|---|
    | **① 在哪台機器** | 正式 VM / 開發者本機 / GitHub UI ——實際值見〈附錄 A.4〉 |
    | **② 在哪個路徑** | 該機器上的絕對路徑,或 repo 根 |
    | **③ 屬於哪個專案** | 哪一個 repo 的指令 |

    格式:指令區塊**前一行**寫成

    ```
    【機器】【路徑】【專案】
    ```

12. **涉及 git 的指令,標明分支等同標明路徑**。同一個路徑在不同分支上是不同的內容;
    只說「在 repo 根下 `git pull`」而不說分支,拉到的可能不是你以為的那份。

🔴 **為什麼這是硬性要求,不是排版偏好:**

同一次工作會橫跨**至少兩個 repo 與三個地方**,而**指令長得一模一樣**:

- `docker compose up -d` 在本服務的部署目錄與 gateway 的部署目錄是**兩件完全不同的事**,
  後者會動到綁 80/443、全 VM 唯一的入口。
- `git pull` 在哪個 repo、哪條分支,決定你拉到的是誰的變更。
- 路由設定的權威**不在本 repo**(在 portal),對本 repo 改 gateway 設定不會有任何效果,
  **也不會有任何錯誤訊息**。

**貼錯地方的代價是動到正式站,而錯誤不會當場顯現。** 標題那一行是唯一擋在中間的東西。

> 前例(portal 2026-08-03 一天內踩到兩次,已寫進其憲法 v1.9):
> ①打 tag 的指令只寫「在你本機或 UI」,被貼到 VM 的部署目錄——那不是 git clone,三行全部 `fatal: not a git repository`;
> ②gateway 套用的指令沒交代 VM 在哪條分支,`git pull` 回「Already up to date」而設定根本沒變,
> **`nginx -t` 過了、reload 也成功了,但套用完全落空**。

## 第十條:例外——限制贏,形狀不變

1. **被要求解釋或走一遍** → 完整解釋。篇幅隨題目需要。仍然無前言、無收尾語。加標題方便回頭掃。
2. **破壞性動作在前**(`rm -rf`、force push、schema migration、drop table)→ 動手前先確認。安全勝過簡短。
3. **連三輪「還是壞的」** → 停止改程式。指出可能錯的那個假設。問一個診斷問題。
4. **需求真的有歧義** → 一個短的釐清問題勝過猜完再重寫。
5. **規則會把答案本身刪掉** → 任務贏。「我有哪些選項」要 2–4 個排序過的選項 + 一行取捨,
   建議放第一個,不是只給一條路。選項就是答案。
6. **代理人自己該做的工作** → 自己做,不要丟回來。有工具就自己改,改完報已驗證的結果。
   「結尾給下一步」不等於把自己的工作外包給人類。

## 第十一條:送出前自檢

送出前刪掉:

1. 第一句,如果它在宣告你接下來要做什麼。
2. 最後一句,如果它在總結剛發生的事或問「還有其他事嗎」。
3. 任何「順便一提」的側欄。
4. 不帶資訊的模糊副詞(「或許」「可能」)。保留承載真實不確定性的 hedge——刪掉它是製造假信心。
5. 成語與比喻(「回頭再看」「動起來」「達成共識」)。用字面上的動作。

然後驗證:只讀第一行和最後一行,我知不知道(a)接下來要做什麼、(b)剛剛發生了什麼?

---

# 附則:共通紅線(不分專案,一律生效)

**誠實**

- 🔴 未實際跑過的測試不得宣稱通過。
- 🔴 尚未接上的能力必須誠實標示未完成狀態(例:掃毒未接上時標 `not_scanned`),不得預設樂觀值。
- 🔴 事後補寫的計畫必須在日誌開頭明示違反第二條,不得偽裝流程跑過。
- 🔴 版本號常數與 tag 必須一致。

**資料**

- 🔴 上線後任何動到資料庫的操作,**動手前必先備份並於 staging 演練**。
- 🔴 動資料的 migration 必須提供 backward(可回滾),且先在 staging 雙向演練。
- 🔴 測試與 CI 一律用 fixture 假資料,不放真實個資或正式資料匯出檔。
- 🔴 log 走 stdout、單行 JSON,不記密碼、完整 token、個資。

**git 與部署**

- 🔴 secret、`.env`、私鑰絕不進 git(`.env.example` 只列變數名)。
- 🔴 `*.sql`、`*.xlsx`、上傳檔目錄、`node_modules/`、`.venv/` 不進 git。
- 🔴 production 不用 `latest` tag;部署是改 tag 後 pull,不在正式機 `git pull && build`。

---

# 附錄 A:專案特化區

## A.1 專案概述

- **系統:** cats-inbox — Sporton 統一入口平台的**站內信 / 通知中心**:系統通知 + 公告(階段一)、人對人站內信(階段二);**不做即時聊天**。獨立服務,掛統一入口子路徑 **`/inbox/`**(D2″ 單一 hostname `catsapp.sporton.com.tw` 之下)。功能上游=cats-portal《站內信通知中心設計規劃》v1.3;**選址已由「掛 compliance_system」變更為獨立 repo**(Benny 2026-08-15 指示),理由與回寫義務見 `docs/開發計畫書.md` §1。
- **技術:** Python 3.13 + FastAPI + Jinja2(伺服器端算繪)+ PostgreSQL 15(平台資料庫版本紅線);容器部署於 CATS VM,經 `portal-gateway` 反代;身分=Keycloak OIDC(realm `sporton`)。刻意極簡:無 Redis、無訊息佇列、無 WebSocket——未讀鈴鐺用 30 秒輪詢。
- **開發藍圖:** 依 `docs/開發計畫書.md` 與 `docs/任務表.md` 的里程碑與任務編號執行(M0–M6、T00–T17)。
- **正式資料狀態:** 尚未上線(上線後〈附則〉資料紅線全面適用)。
- **MVP 不做:** 即時聊天(WebSocket / 已讀回條 / 線上狀態)、SSE 推播、Email 併寄(N2 待決)、附件、全文檢索、行動 App。

## A.2 上位規約與整合契約

| 契約 | 權威位置 | 本專案已對齊版本 | 對齊紀錄 |
|---|---|---|---|
| 帳號系統接入契約(SSO/OIDC) | cats-portal `DOCS/帳號系統接入契約_SSO.md` | **v2.7(2026-08-10)** | `docs/開發計畫書.md` §2.1;dev-log `2026-08-15_T00` |
| App 服務對齊指南(gateway / cats-edge) | cats-portal `DOCS/App服務對齊指南_portal-gateway整合.md` | **v1.3(2026-07-27)** | `docs/開發計畫書.md` §2.2 |
| 站內信通知中心設計規劃(功能上游 what/why) | cats-portal `DOCS/站內信通知中心設計規劃.md` | **v1.3(2026-07-27)**;⚠ 選址變更待回寫(T02) | `docs/開發計畫書.md` §1 |
| portal 平台紅線(D2″/D8/PG15/容器紅線) | cats-portal `CLAUDE.md` | **v1.11(2026-08-05)** 之相關紅線 | `docs/開發計畫書.md` §2、本檔 A.3 |
| S2S(service-to-service)章節 | 同 SSO 契約 §6(「首個需求出現時補章節」) | ⏳ **未定稿**——rf-workorder 已於 2026-08-14 送件、報告產生系統排隊中;cats-inbox 推送 API 為第三個需求 | T13 跟進送件 |

- 上述文件之修改一律走 cats-portal 的 PR;本 repo 只引用、不複製維護。
- 🔴 SSO 契約 §4.10 同源義務(cookie Path、嚴格 CSP、前端 base path、禁 token 進 localStorage、**禁外部 CDN**)對本專案有強制力。
- 🔴 SSO 契約 §10 SLO:本專案 session 形態=**cookie / 伺服器 session**,front-channel logout 端點為 **MUST**。

## A.3 專案紅線

**上位平台鐵則(偏離需 portal/Benny 同意)**

- 🔴 不自建帳號系統、不存密碼、不自簽 JWT——一律 Keycloak OIDC(Authorization Code + **PKCE**、**RS256 + JWKS**(快取 1h、支援 kid 輪替)、驗 `iss`/`aud`/`exp`、±30s);401=token 無效、403=已認證未授權,不得混用。
- 🔴 業務庫只存 **`sub`** + 本地角色;顯示名稱快取僅得依契約 **§4.2a L1**(僅自本人登入 token 取得、**僅供管理後台顯示**、不進 log、nullable、附整批清除工具)。**收件匣對一般使用者顯示寄件人姓名,現行契約無依據——DI-3 裁決前不得實作**(見開發計畫書 §2.3)。
- 🔴 容器:只有對外容器 `cats-inbox-api` 上 `cats-edge`(寫進自家 compose,`external: true` + `name: cats-edge`);**`cats-inbox-pg` 不上 `cats-edge`、不對主機發布 port**;全 VM 只有一個 gateway,路由加改一律走 cats-portal PR;換版重建後必 `docker exec portal-gateway nginx -s reload`;絕不啟動舊的 `nginx-gateway`。
- 🔴 PostgreSQL **15**(平台統一);不跨 DB join,跨系統互通一律 HTTP API;禁 `build: .`,一律 prebuilt image;健康檢查與 restart policy 必配。
- 🔴 首登自動建本地 user(零角色)→ 業務功能 403「待開通」(頁面顯示本人 `sub`);deny-by-default;保留自家停用能力(D9=B 之後離職不會自動失效)。

**本專案自我加嚴(站內信=使用者輸入直接展示給其他人,風險自負)**

- 🔴 訊息 `body` 一律**純文字**儲存與輸出(輸出必跳脫);不支援使用者提供之 HTML——stored XSS 是本系統第一風險,且同源之下(§4.10)一次 XSS 可觸及 IdP。
- 🔴 `action_url` 只接受**同站**值(`/` 開頭之相對路徑,或 `https://catsapp.sporton.com.tw/` 前綴),否則 400 拒收——站內通知是現成的釣魚載具。
- 🔴 訊息主旨與內容**不進 log**;log 只記 id、sub、事件類型(共通紅線之個資條的本專案落點)。
- 🔴 `sender_sub` 一律由後端自 token 取得、收件權限一律後端判定(只能讀 `recipient_sub == 自己`),前端傳入的身分欄位一律不採信。
- 🔴 推送 API 必須驗 S2S token(client_credentials);**無驗證的推送端點不得部署到正式環境**——不得以「先用共享 API key 頂著」上線。

## A.4 慣例

- **任務編號格式:** `Tnn`(例:T01) **里程碑格式:** `Mn`(例:M1)
- **註釋語言:** 中文(第五條預設)
- **文件目錄:** `docs/plans/`、`docs/dev-logs/`、`docs/assets/`
- **測試指令:** `bash tests/run_all.sh`
- **HTML 產生指令:** `python3 tools/render_docs.py`(`--check` 供 CI 驗同步)
- **指令位置標示的實際值(第九條11 的 ①②③ 在本專案填什麼):**

  | 標的 | 本專案可能的值 |
  |---|---|
  | ① 機器 | `CATS VM(Ubuntu)` / `開發者本機` / `GitHub UI` |
  | ② 路徑 | `/opt/cats-inbox`(本服務部署目錄)、`/opt/cats-portal`(**gateway 權威設定,動它一律走 portal 的 PR**)、開發者本機的 repo 根、涉及 git 時附分支名 |
  | ③ 專案 | `cats-inbox` / `cats-portal` |

  例:`【CATS VM(Ubuntu)】【/opt/cats-inbox】【cats-inbox】`
  🔴 **本專案最容易貼錯的一組**:換版重建後要下的 `docker exec portal-gateway nginx -s reload`
  是在 **VM** 上、動的是 **portal 擁有的 gateway**——不是在本機、也不是本 repo 的東西。

- **交付使用者下載的文件副本,檔名加 `cats-inbox_` 前綴**(沿 portal 第四條6 的教訓:下載目錄裡分不出誰發的);repo 內正式文件鏈檔名依本憲法固定(`docs/開發計畫書.md` 等),不加前綴、不帶日期。

---

# 附錄 B:採用清單(新專案 5 步)

1. 複製本檔到 repo 根目錄,命名 `CLAUDE.md`,填上建立日期時分與版本歷史第一列。
2. 填〈附錄 A.1〉專案概述與〈A.4〉慣例——這兩節不填,第一條與第六條無法執行。
3. 有上位平台就填〈A.2〉並記錄對齊版本;沒有就把〈位階〉圖最上層刪掉。
4. 填〈A.3〉專案紅線;逐條問「違反這條會不會炸到別人」,會的才留。
5. 建 `docs/plans/`、`docs/dev-logs/`、`tests/` 三個空目錄,提交第一個 commit。

---

## 版本歷史

| 版本 | 日期 | 修改人 | 摘要 |
|---|---|---|---|
| v1.1 | 2026-08-15 | Benny + Claude | **第九條新增第 11、12 款:指令必須標明「在哪裡下」**(Benny 指示)。任何 shell/docker/git/SQL 指令前須先標 ①機器 ②路徑 ③專案,格式 `【機器】【路徑】【專案】`;涉及 git 時標明分支等同標明路徑。理由:同一次工作橫跨兩個 repo 與至少三個地方,而**指令長得一模一樣**——`docker compose up -d` 在本服務目錄與 gateway 目錄是兩件事(後者動到綁 80/443 的唯一入口);且**路由權威不在本 repo**,對本 repo 改 gateway 設定不會生效也不會報錯。**貼錯地方的代價是動到正式站,而錯誤不會當場顯現**(前例:portal 2026-08-03 同日踩兩次,其憲法 v1.9 已記載——`nginx -t` 過、reload 成功,而設定根本沒變,因為 VM 在另一條分支上)。🔴 條文**附在第九條 1–10 之後**而非插入中間:插隊會讓既有引用悄悄指到別的東西。機器/路徑的**實際值填在〈附錄 A.4〉**,不寫進條文——條文須維持可跨專案原封複製(本檔導言的設計原則)。升次版號:新增條款,非改方向 |
| v1.0 | 2026-08-15 | Claude(Benny) | 初版:採用通用開發憲法 v1.0(2026-08-05 版,卷一八條+卷二三條+共通紅線,條文原封沿用);依附錄 B 五步特化——〈位階〉最上層填入實際上位規約(SSO 契約/對齊指南/portal 紅線),A.1 填 cats-inbox 概述(站內信/通知中心、獨立服務、掛 `/inbox/`),A.2 記錄對齊版本(SSO v2.7、指南 v1.3、設計規劃 v1.3、portal 憲法 v1.11、S2S 章節未定稿),A.3 填上位鐵則與五條自我加嚴(純文字 body、action_url 同站白名單、內容不進 log、身分不信前端、推送必驗 S2S),A.4 填慣例;移除範本內 upload-program 範例區塊 |

<!--
卷二 distilled from github.com/ayghri/i-have-adhd (MIT), by Ayoub G.
Loosely based on The Adult ADHD Tool Kit, Ramsay & Rostain.
-->
